# Phase 4a — Implementation Progress

**Worktree:** NONE — in-place on the active shared branch (the established engine workflow; Phase 3b/3c worked this way too; `.agents/atx-engine/agent.md` is the authority).
**Branch:** `feat/atx-core-stdlib`
**Base:** `feat/atx-core-stdlib` @ `bf4cbfb`
**Started:** 2026-06-06
**Source plan:** [`phase-4-signal-combination-risk-implementation-plan.md`](phase-4-signal-combination-risk-implementation-plan.md)
**Prior progress:** Phase 3c ([`phase-3c-progress.md`](phase-3c-progress.md))

---

## Plan adjustments vs. the source plan

Phase 4 is split into two sprints: **4a** (this ledger, P4-0…P4-5 — alpha pool + gates + combiner) and **4b** (P4-6…P4-10 — risk model, optimizer, portfolio integration). The source plan §1–§11 is frozen; this sprint covers only the 4a scope.

The plan carries an **amendment §0 (Phase-3 as-built reconciliation, 2026-06-06)** that overrides the frozen §1–§11 wherever they disagree. The 4a-relevant deltas are: **P4-1** wraps the as-built `alpha::AlphaStreams` + `extract_streams` API rather than rebuilding dense signal matrices from scratch — `AlphaRecord` holds a compiled `Program` and the store's streaming interface is layered on the existing Phase-3 VM output; **P4-2** computes metrics over `AlphaStreams::pnl(a)` and treats `pnl[0]=0` as a structural zero (the first bar is always NaN-equivalent for a causal signal, so any Sharpe/IC formula must skip it); **P4-4** the combiner's input pool is a batch `SignalSet → extract_streams` pass — weights are fit on the constant-weight stream (equal-weight baseline first, IC-weighted second), keeping the hot path allocation-free; **P4-5** the production form is a single `compile_batch` Program adapter (`CombinedSignalSource`) whose `max_lookback()` forwards `Program::required_lookback` (a field, not a virtual call), and a `vector<ISignalSource*>` form is retained for unit tests (avoids pulling the full combiner into test fixtures).

Realistic scope for this sprint:

1. **P4-0** — Module scaffold + CMake + ledger (marker). Create `combine/fwd.hpp`, trivial scaffold test, open ledger. No combination logic.
2. **P4-1** — `AlphaStore`: insertion-ordered registry of live alphas (`AlphaId`, `AlphaRecord`, `AlphaStore`), wrapping the as-built `alpha::AlphaStreams` + `extract_streams`.
3. **P4-2** — `AlphaMetrics`: per-alpha fitness statistics (IC, turnover, Sharpe, max-drawdown, pairwise-correlation summary) computed over `AlphaStreams::pnl(a)`, `pnl[0]=0` structural-zero convention.
4. **P4-3** — `AlphaGate`: stateless fitness screen (`GateConfig` thresholds → `GateVerdict`) filtering the pool on IC floor, max pairwise correlation, max turnover, min Sharpe.
5. **P4-4** — `AlphaCombiner`: weight-fitting over admitted pool (`CombineMethod`, `CombinerConfig`, `Combination`); equal-weight baseline + IC-weighted; batch `SignalSet → extract_streams` hot path.
6. **P4-5** — `CombinedSignalSource`: `compile_batch` Program adapter implementing `ISignalSource`, `max_lookback()` = `Program::required_lookback`. Sprint close.

Defer to Phase 4b (or later):

- **P4-6…P4-10** — Barra-style risk model, portfolio optimizer, full Phase-2 loop integration, live-trading adapter — in the 4b ledger.

---

## Per-unit ledger

| Unit  | Status | Commit  | Notes |
|-------|--------|---------|-------|
| P4-0  | done   | `44d88a8` | scaffold + ledger; combine_scaffold_test green (CombineScaffold 1/1/0/0) |
| P4-1  | done   | `b054436` | `combine/metrics.hpp` (AlphaMetrics POD only — P4-2 adds compute) + `combine/store.hpp` (AlphaId, AlphaRecord, AlphaStore). Append-only, insertion-ordered pool owning each alpha's PnL row + per-period position stream + a NON-OWNING `atx::engine::ISignalSource*` re-eval handle (forward-declared). **§0-A wrap-not-rebuild:** insert() COPIES pre-computed `extract_streams` rows into the store's own id-ordered dense matrices — it never recomputes PnL; `ingest_streams(AlphaStreams, sources, metrics)` is the "one extract_streams call" batch path (one insert per row in id order). `pnl_matrix()` is **alpha-major row-major `[n_alphas × n_periods]`** (element (a,t) at `a*n_periods+t`; row a == `pnl(id)`); first insert fixes n_periods/n_instruments, later period-count mismatch → `Err(InvalidArgument)` (store unchanged). NaN streams stored verbatim. `ingest_streams` tested against a hand-built `alpha::AlphaStreams` (aggregate-constructible, no extract_streams pipeline needed). AlphaStore 8/0/0 green; /W4 /permissive- /WX clean. |
| P4-3  | done   | `67a248b` | `combine/correlation.hpp` (shared `pairwise_complete_corr`) + `combine/gate.hpp` (`GateConfig`, `GateVerdict`, `AlphaGate`). `AlphaGate::admit` is `[[nodiscard]] const noexcept`: §5.2 FIXED-ORDER deterministic verdict — first failing condition wins, checked Sharpe→fitness→turnover→correlation. **LAZY-CORR optimization (mandated):** `corr_to_pool` (the O(\|pool\|·T) cost) is computed ONLY after the three floors pass; observationally identical verdict to the eager order, but a floor-rejected candidate never pays the corr cost. `corr_to_pool = max_j \|pairwise_complete_corr(candidate, member_j)\|` — MAX = strictest member, MAGNITUDE gate (a perfect copy +1 and a perfect anti-correlation −1 are equally disqualifying); empty pool ⇒ 0 ⇒ first floor-clearing alpha always admitted. **SHARED corr helper decision:** atx-core exposes only a fixed-window `RollingCorrelation<W>` (no pairwise-complete NaN skipping), so I IMPLEMENTED the helper (single-pass Σa,Σb,Σab,Σa²,Σb²,n over both-non-NaN pairs; Pearson `(n·Σab−Σa·Σb)/sqrt((n·Σa²−Σa²)(n·Σb²−Σb²))`, clamped to [−1,1]) rather than wrapping — it lives in its OWN header (`combine/correlation.hpp`) so P4-4's covariance estimate reuses the EXACT same definition + one documented §3.3 policy. **Degenerate-pair convention (documented, load-bearing):** `< 2` valid pairs OR zero variance in either leg ⇒ return `0.0` — a degenerate/no-overlap pair carries no co-movement info, so it can never trip the magnitude gate falsely. **Index-0 choice:** followed §5.2 VERBATIM — index 0 (the AlphaStreams structural 0) is non-NaN, so it IS a valid pair and is INCLUDED (no special-casing); Pearson is mean-centered so a shared leading point has negligible effect, and this deliberately differs from P4-2's MOMENT exclusion (a structural 0 biases mean/var but not a correlation). **All-NaN candidate** ⇒ 0 valid pairs vs every member ⇒ `corr_to_pool = 0` ⇒ PASSES the corr gate ⇒ Accepts when the floors are cleared (the floors, checked first, are responsible for catching junk — pinned by a test). `GateVerdict : atx::u8` matches the `fwd.hpp` forward decl; enumerator order mirrors the §5.2 check order. **Release-OOB hardening (code-quality review):** `pairwise_complete_corr` now bounds its loop by `min(a.size(), b.size())` so a mismatched-length call truncates to the overlap instead of reading OOB in release (the equal-length `ATX_ASSERT` still fires loudly in debug — "fail loud in debug, fail safe in release"); the gate path adds a debug `ATX_ASSERT(pool.n_periods()==0 || candidate.size()==pool.n_periods())`. 20 new tests (`Correlation` 8/0/0 + `AlphaGate` 12/0/0, incl. fixed-order sharpe-before-turnover, NaN-gap pool overlap, max-over-pool strictest, boundary-at-threshold, and a `MismatchedLengthAbortsInDebug` EXPECT_DEATH pinning the contract). (NB: a broad `ctest -R "...|Correlation"` run reports more because the regex ALSO matches pre-existing `*.Correlation` alpha-DSL tests; the new-test count is 20.) Full engine suite green; /W4 /permissive- /WX clean. |
| P4-4  | done   | `fb5d69a` | `combine/combiner.hpp` (`CombineMethod : atx::u8`, `CombinerConfig`, `Combination`, `AlphaCombiner`) + a `pairwise_complete_cov` helper added ALONGSIDE `pairwise_complete_corr` in `combine/correlation.hpp` (the §3.3 NaN-policy covariance analog — one shared policy). `AlphaCombiner::fit(pool, fit_begin, fit_end)` is `[[nodiscard]] const`, returns `Result<Combination>`, COLD path (allocates scratch). **§3.1 firewall (provable):** every per-method helper reads ONLY `pnl(id).subspan(fit_begin, T)` — no code path indexes a row ≥ fit_end; the truncation-invariance test fits on `[1,4)` over two pools that differ ONLY in rows ≥ 4 and asserts BYTE-IDENTICAL weights. `Combination` carries `[fit_begin, fit_end)` (the apply-side rail P4-5 enforces). **EXHAUSTIVE `switch (cfg.method)` — NO default** (a new enumerator is a compile error). All five §5.3 methods implemented + tested: **EqualWeight/RankAverage** closed-form uniform `1/N` (RankAverage returns uniform here — the METHOD flag, carried to P4-5, is what differs; rank-space combine is P4-5's job); **IcWeighted** uses the plan's **return-only IC proxy** `IC_i ≈ window-sharpe_i = mean/std (POPULATION) recomputed over [fit_begin,fit_end) from the sub-span` (NOT the stored `AlphaMetrics.sharpe`, which would leak the full stream past the window), `w_i ∝ max(sharpe_i,0)`, `Σw=1`, all-non-positive → uniform fallback; applies the **P4-2 §0-F structural-zero exclusion** (index 0 dropped from the per-alpha moment IFF `fit_begin==0`); IC-from-signal deferred (no raw signal in the store); **ShrinkageMv** (default) `Σ̂=(1−ρ)S+ρ·(tr(S)/N)·I`, `S` = pairwise-complete sample covariance (sample N−1 divisor) built entry-by-entry via the new `pairwise_complete_cov`, solve `Σ̂w=μ` via atx-core `linalg::solve_spd` (Cholesky), renormalize `Σ|w|=1`, **Err(Internal)** if Σ̂ not SPD even after shrinkage; **BoundedRegression** ridge-regularized MV in the **top-k PC space** via atx-core `linalg::pca` of the complete-case column-demeaned window (`k=cfg.n_pcs` or all if 0), `w_pc=(Λ_k+ridge_lambda)⁻¹(V_kᵀμ)`, `w=V_k·w_pc`, then **clip `|w_i|≤weight_bound` + renorm over a FIXED 4 passes** (no convergence-dependent exit — §3.2 determinism; residual marginal overshoot is bounded, test tol `1e-6`), return-space realization (signal-space bounded regression deferred — needs raw signals). **Ledoit-Wolf:** atx-core has NO LW/shrinkage helper (grepped `linalg/`+`stats/`), so the standard **LW 2004** closed-form intensity is implemented (`m=tr(S)/N`; `d²=‖S−m·I‖²_F/N`; `b̄²=(1/N)·meanₜ‖xₜxₜᵀ−S‖²_F` clamped ≤ d²; `ρ*=clamp(b̄²/d²,0,1)`) over complete-case demeaned rows, order-fixed sums (deterministic); `cfg.shrinkage<0` ⇒ AUTO LW, else fixed intensity clamped to `[0,1]`. **Matrix convention matched from atx-core:** column-major Eigen `MatX`/`VecX`, `solve_spd(MatX,VecX)→Result<VecX>`, `pca(X n_samples×n_features, k)→PcaResult{components N×k, explained_variance}`, `symmetric_eig` ascending — no hand-rolled eigen/Cholesky. **Boundaries:** single alpha short-circuits to `w=[1]` before any solve (cannot fail); empty pool → `Err(InvalidArgument)`; `T<2` → `Err(InvalidArgument)`; `fit_end>n_periods` → `Err(OutOfRange)`. NaN entries skipped pairwise-complete throughout. **AlphaCombiner 15/0/0** new tests (verified via `--gtest_list_tests --gtest_filter=AlphaCombiner.*` = 15; full engine suite 1494/1494). /W4 /permissive- /WX clean. **(AMENDED by `0c9e683` — see below: the LW normalization above (`d²/N`, `b̄²/(N·T)`) was a DEFECT that saturated ρ→1; corrected to the canonical 1/T² form and the SCM switched to complete-case MLE.)** |
| P4-4-fix | done | `0c9e683` | **Ledoit-Wolf normalization defect fix (spec review).** The P4-4 LW intensity used `d²=‖S−μI‖²_F/N` and `b̄²=(1/(N·T))Σ‖xₜxₜᵀ−S‖²_F`, which is off by ~T/N — `b̄²` saturated against `d²` so `ρ→1` (full shrinkage to μ·I) on essentially every realistic window, collapsing ShrinkageMv to EqualWeight and discarding the SCM off-diagonal/variance structure. **Corrected to the canonical Ledoit & Wolf 2004 closed form (1/T² normalization):** `μ=tr(S)/N`; `d²=‖S−μ·I‖²_F` (NO 1/N); `π̂=(1/T_cc)Σₜ‖rₜrₜᵀ−S‖²_F`; `b̄²=π̂/T_cc` (== `(1/T_cc²)Σ‖·‖²_F` — the missing 1/T); `b²=min(b̄²,d²)`; `δ=clamp(b²/d²,0,1)`. **Coherence fix (folded in):** ShrinkageMv now uses ONE **complete-case (listwise) MLE covariance** `S=(1/T_cc)·Σₜ rₜrₜᵀ` (divisor T_cc, demeaned window, periods with ANY NaN dropped) for BOTH the LW intensity AND the shrink/solve — the prior mix (pairwise_complete_cov S for the solve, listwise rows for the intensity) was incoherent once ρ<1. The `pairwise_complete_cov` helper previously added to `combine/correlation.hpp` was thus orphaned by this coherence move (combiner.hpp also drops the correlation.hpp include); for a no-NaN window the two S agree. Removed the now-dead `window_covariance` helper. (The orphaned `pairwise_complete_cov` was REMOVED in the follow-up cleanup `355be0d` — see below.) **SPD floor (`kLwSpdFloor=1e-6`, AUTO path only):** the canonical LW δ can come out ~0 on a SINGULAR S (T_cc<N: the populated subspace looks near-spherical → little shrinkage signal → δ≈1.6e-32 on the T=2,N=3 fixture, leaving a 0 null-space eigenvalue and a failed Cholesky), but the contract requires shrinkage to RESCUE T<N; the AUTO δ is floored to 1e-6 — a NUMERICAL SPD guard (lifts every null-space eigenvalue to ≥δ·μ>0, measured 3.75e-10 on the fixture) that stays FAR below any statistically-meaningful δ (well-conditioned LW δ ≫ 1e-6, so the inverse-variance tilt is unaffected); a caller-supplied fixed `cfg.shrinkage` is honored verbatim (no floor). δ exposed via a testable `detail::shrunk_covariance(...)→ShrunkCov{sigma,mu,delta}` helper. **Regression test added** (`ShrinkageMvWellConditionedInverseVarianceTilt`): T=64≫N=3, three mutually-orthogonal Walsh square waves (periods 2/4/8, distinct amplitudes 0.005/0.02/0.05 → distinct variances + ~0 cross-corr) each + equal positive drift → equal μ; asserts (a) NOT EqualWeight (`|w₀−1/3|>1e-3`), (b) inverse-variance tilt `|w₀|>|w₂|` (lowest-var earns more), (c) **δ<0.5** (measured **δ≈0.0094** on this fixture, weights ≈ [0.911, 0.076, 0.012] — i.e. ρ≪1, a strong inverse-variance tilt; the bug's saturation is gone). The identical-alphas (ρ→1 genuinely — rank-1 S) and singular-T<N (rescued by the floor) tests STILL pass. **AlphaCombiner 16/0/0** (15 + this regression test; verified `--gtest_list_tests --gtest_filter=AlphaCombiner.*`=16; full engine suite 1495/1495). /W4 /permissive- /WX clean. |
| P4-4-cleanup | done | `355be0d` | **Code-quality review fixes (2 findings).** (1) **DEAD CODE removed:** `pairwise_complete_cov` in `combine/correlation.hpp` had ZERO callers after the `0c9e683` coherence fix moved ShrinkageMv to complete-case `mle_covariance` (grep confirmed only self-referential comments) — agent.md §9 forbids dead code, so the function and its doc block were deleted, and the two stale "retained for genuinely-pairwise use" mentions in combiner.hpp were rewritten so no dangling reference remains. `pairwise_complete_corr` is UNTOUCHED (still live in gate.hpp — Correlation + AlphaGate suites stay green, confirming it compiles/links). (2) **NARROWING documented:** `window_span`'s `static_cast<atx::u32>(id)` now carries a `// SAFETY:` note — every caller iterates `id` over `[0, pool.size())` and AlphaStore mints only u32 ids (insert() caps the pool at u32-max), so the narrowing provably cannot truncate. No test changed (the cov fn had no test): **AlphaCombiner still 16/0/0** (verified `--gtest_list_tests --gtest_filter=AlphaCombiner.*`=16); Correlation 8/0/0 + AlphaGate 12/0/0 still green. /W4 /permissive- /WX clean. |
| P4-5  | done   | `5147c64` | `combine/combined_source.hpp` (`CombinedSignalSource final : public ISignalSource`) + `tests/combine_combined_source_test.cpp`. **The Sprint-4a CAPSTONE: the mega-alpha as an `ISignalSource`** — wraps the pool's constituent sources + a frozen `Combination` (fit OOS-safely by P4-4; this source only APPLIES the weights) so it runs in the EXISTING `BacktestLoop` with **ZERO loop changes** (the seam Phase-2 anticipated — `loop/*.hpp` untouched). **§0-C form shipped:** the frozen §8 `std::vector<ISignalSource*>` multi-source blend exactly as the ctor specifies (`CombinedSignalSource(std::vector<ISignalSource*> sources, Combination combo, CombineMethod method) noexcept`, moves in). **§0-C production RESIDUAL (NOT built; documented in the header + here, to be lifted to the ROADMAP at 4a close):** the single `compile_batch` Program adapter (CSE-shared, all constituents in one VM pass, `max_lookback()` forwarding `Program::required_lookback`) overlaps `VmSignalSource`; deferred. `max_lookback()` here = **MAX over `sources_[i]->max_lookback()`** (0 if empty), `noexcept`. **Blend (`evaluate`, PURE in panel, EXHAUSTIVE `switch (CombineMethod)` — NO default):** each constituent is evaluated over `panel` (`ATX_TRY` propagates Err) and the borrowed current-date cross-sections are blended per instrument `k` over the surviving set `V_k = {i : s_i[k] not NaN}`. **LINEAR** (EqualWeight/IcWeighted/ShrinkageMv/BoundedRegression): **gross-preserving renorm** `out[k] = (Σ_{i∈V_k} w_i·s_i[k]) / (Σ_{i∈V_k} |w_i|)` — dividing by the SURVIVING GROSS keeps the scale stable when constituents drop out AND is finite for a dollar-neutral combo (Σw=0, gross>0 → no div-by-zero, pinned by `DollarNeutralWeights_GrossRenorm_Finite`); `V_k` empty OR zero surviving gross → `NaN`. RELATIONSHIP TO P4-4: P4-4 gross-normalizes `Σ|w|=1` (`renorm_abs_sum`), so with no NaN the denominator is 1 and this reduces to the plain weighted sum `Σ w_i s_i[k]`. SINGLE CONSTITUENT → `(w_0 s_0[k])/|w_0| = sign(w_0)·s_0[k]` (negative weight flips the sign, magnitude unchanged; zero-weight → NaN). **RankAverage** combines in RANK space: `out[k] = mean_{i∈V_k} rank_i[k]`, weights IGNORED; the per-constituent rank is the cross-sectional ordinal-percentile `r/(n_valid−1)` (ascending value, NaN-last, stable index tie-break, singleton→0.5) — the **Phase-3 CsRank convention** reproduced by a **LOCAL** `detail::cs_percentile_rank` (NOT a call into `alpha::detail::cs_rank_row`, which lives in the ODR-fenced oracle/VM differential-test namespace; §8 explicitly permits a local deterministic rank — it matches cs_ops.hpp's r/(n−1) policy exactly). **SignalView returned EXACTLY as `VmSignalSource`** — `Ok(SignalView{std::span<const f64>{out_…}})` over the source-owned `out_` blend buffer, grown-only (steady-state no realloc — the seam forbids hot-path alloc); rank/sum/count/order scratch likewise sized once per shape. **NON-OWNING constituents** (`ISignalSource*`; caller owns lifetime — documented). **Determinism §3.2:** fixed constituent order (ascending i) + fixed instrument order (ascending k), stable rank tie-break, no RNG. **Real-BacktestLoop integration proof (the capstone):** `RunsInRealBacktestLoop_MatchesPhase2SingleAlpha` mirrors `backtest_loop_test.cpp::run_backtest` VERBATIM (Phase-1 spine `InMemoryBarFeed→SimClock→EventBus` → `RollingPanel` → SIGNAL SOURCE → `WeightPolicy` → frictionless `ExecutionSimulator` → `Portfolio` → `BacktestResult`), substituting ONLY the signal source: a `CombinedSignalSource` over **two REUSED `ScriptedSignalSource` constituents** (no stub written) each replaying `[3,2,1]`, EqualWeight `w=[0.5,0.5]` → blend `[3,2,1]` every rebalance, so the loop reproduces the Phase-2 single-alpha end-to-end result EXACTLY (A=+500/B=0/C=−250, two opening fills decided at slice 0 / filled at slice 1, frictionless dollar-neutral equity≡100,000); `RealLoop_RepeatRun_IsByteIdentical` pins byte-equal fills/qty/equity on a repeat run. **CombinedSignalSource 13/0/0** new tests (verified via `--gtest_list_tests --gtest_filter=CombinedSignalSource.*` = 13). Full engine suite 1511/1511. /W4 /permissive- /WX clean. |
| P4-2  | done   | `16270d6` | `compute_metrics(...)` added to `combine/metrics.hpp` (pure, `[[nodiscard]]`, `noexcept`, single-pass per statistic, no heap). Signature: `compute_metrics(std::span<const f64> pnl, std::span<const f64> positions_flat, usize n_instruments, f64 book_size)`. Computes §5.1 (reconciled with §0): Sharpe `sqrt(252)*mean/std`, returns `252*mean`, turnover `mean(u)`, margin `returns/max(turnover,1e-9)`, WQ §4.4 fitness `sqrt(abs(returns)/max(turnover,0.125))*sharpe`, holding `1/max(turnover,1e-9)`, drawdown over `equity=cumprod(1+r)`. **§0-F structural-zero decision:** index 0 of `AlphaStreams::pnl(a)` is a STRUCTURAL zero (no prior weight/close) → mean/std/returns computed over `r[1..T)` ONLY (Welford via atx-core `RunningVariance`, **population** variance to match §5.1's `std`); the drawdown walk DOES start at period 0 (r[0]=0 keeps equity at 1.0, harmless). **Turnover derived from positions** (AlphaStreams exposes no turnover stream): `u[t]=Σ|Δw|/book` with **`u[0]=Σ|w[0]|/book`** (trade-in-from-flat), NOT excluded from `mean(u)`. NaN policy: flat stream (std==0) → sharpe 0; turnover==0 ∧ returns==0 → fitness 0; **length-1 / empty stream → 0 return observations → NaN sharpe/returns** (documented), drawdown 0; all-NaN stream → NaN moments. Named constants `kAnnualizationDays`/`kTurnoverFloor`/`kEps`. Also corrected the stale `fwd.hpp` AlphaMetrics doc (it wrongly listed IC + correlation; the POD has neither — those live in the gate/combiner). AlphaMetrics 12/0/0 green; full engine suite 1459/1459; /W4 /permissive- /WX clean. |

---

## Phase 4a sprint commits

| Commit  | Unit   | Test counts |
|---------|--------|-------------|
| `44d88a8` | marker (P4-0) | CombineScaffold 1/1/0/0 |
| `b054436` | P4-1 (alpha store + record) | AlphaStore 8/0/0 |
| `16270d6` | P4-2 (per-alpha metrics + fitness) | AlphaMetrics 12/0/0 |
| `67a248b` | P4-3 (orthogonality + quality gates) | Correlation 8/0/0 + AlphaGate 12/0/0 (20 new tests; +1 EXPECT_DEATH from the release-OOB fix) |
| `fb5d69a` | P4-4 (progressive alpha combiner) | AlphaCombiner 15/0/0 |
| `0c9e683` | P4-4-fix (Ledoit-Wolf 1/T² normalization + complete-case MLE SCM + SPD floor) | AlphaCombiner 16/0/0 (+1 regression test; measured δ≈0.0094 on the well-conditioned fixture) |
| `5147c64` | P4-5 (combined signal source — the mega-alpha, Sprint-4a CAPSTONE) | CombinedSignalSource 13/0/0 (incl. the real-BacktestLoop integration test reproducing the Phase-2 single-alpha result + a byte-identical repeat-run determinism test) |

---

## What Phase 4a proves / Next

_(Fill at sprint close.)_
