# Warehouse Parity Handoff

This handoff covers the US-equity fundamentals/public-alt-data warehouse parity goal. The working rule remains: do not touch C++/CMake unless the user explicitly redirects; continue in `atx-impl/db` and `atx-impl/scripts`, using `archive/research` and `.superpowers/sdd/progress.md` as the spec/progress spine.

## Current State

- S4 fundamentals depth is complete in code: canonical statement map coverage, bank/insurance/REIT overlays, four-date period model (`datadate`, `rdq`, `pdate`, `fdate`, `ldate`), and SQL XBRL calculation-linkbase validation.
- S5a-S5c pricing/corporate-action spine is complete in code: PIT shares outstanding, action-type dimensions, event-level adjustment factors, daily cumulative adjustment factors, and adjusted daily-panel return columns.
- S5d-S5e delisting spine is complete in code for public evidence and injectable observed terminal returns: `delist_code_dim`, `delisting_events`, and `delisting_return_observations`. Default returns remain null unless a licensed/manual file is loaded or an explicit imputation option is used.
- S5f-S5k estimates injection, reconciliation, and public guidance extraction surfaces are complete in code for detail, consensus snapshots, recommendation/price-target events, aggregate recommendation/price-target summary snapshots, PIT-safe estimate vendor-security-id links, and SEC 8-K guidance text extraction: `est_detail` loads IBES-like broker/analyst detail rows; `est_consensus` loads IBES `statsumu`-style or normalized summary snapshots; `est_recommendation` loads IBES `recddet`/`ptgdet`-style events; `est_recommendation_summary` loads IBES `recdsum`/`ptgsum`-style or normalized aggregate recommendation distributions and price-target summaries; `est_security_link` reconciles estimate vendor identifiers (`IBES_TICKER`, CUSIP, FactSet fsym, CIQ trading item, etc.) to `security_id` using only PIT-visible evidence; `est_guidance` now accepts local SEC 8-K Item 2.02/7.01 text corpora via `--estimate-guidance-file`, extracts conservative revenue/EPS/operating-income/net-income ranges with confidence and evidence text, and preserves source-file hashes.
- S6a 13F filer entity-resolution is complete in code and populated from public data: `filer_13f_cik_alias` (`db/filer_alias.py`) is a bitemporal CIK alias / subadvisor / M&A-continuity spine. The auto-derived layer is high precision (SELF identity per filer CIK + intra-CIK `NAME_HISTORY`); cross-CIK same-name links are emitted only as low-confidence `NAME_MATCH_CANDIDATE` rows; authoritative rollups arrive via an injectable curated seed CSV (`--seed-file`). `resolve_primary_cik` defaults to authoritative-only (confidence >= 1.0) so unrelated same-named filers never silently merge. Live build over the 10,672-CIK manager universe produced 10,672 SELF + 99 candidate rows (real multi-CIK filers: Dimensional Fund Advisors across 3 CIKs, D.E. Shaw, Citigroup, Northern Trust).
- S6b normalizes the 2023-01-03 SEC 13F `VALUE`-unit cutover (`thirteenf.value_unit_multiplier` / `apply_value_unit_cutover`): VALUE is scaled ×1000 to whole dollars for `period_of_report <= 2022-12-31` at holdings load. No live row change (all loaded periods post-2023); protects future pre-2023 backfills from a 1000× SUM understatement.
- S7a/S28 FINRA OTC Transparency off-exchange surfaces are complete in code as an injectable slice: `offexchange_venue` (MPID dim), `offexchange_volume` (per security/venue/period fact with bitemporal availability + FINRA restatement handling via `is_latest`/`restatement_seq`), `offexchange_security_period` (derived ATS-share rollup), `offexchange_quality_report` (period-level control/quality report across OTC transparency and daily short-volume inputs), and `v_offexchange_security_period` (`db/offexchange.py`, `db/offexchange_quality.py`). The loader ingests a FINRA `weeklySummary`-shaped or normalized CSV; an end-to-end smoke reproduces the blueprint AAPL fixture exactly (ats_share_pct 31.7470%). Default DB tables are empty until FINRA files are injected; the live OAuth2 FINRA Query API connector is not yet wired (no network in tests).
- S7b (migration 0031) catalogued the four reference-classification tables and the off-exchange view, closing the standing `missing_table_catalog_entries`/`missing_field_catalog_entries` quality failures.
- S8a makes reference classification populatable without network: `EntityClassificationDataset` accepts an injectable CIK→SIC CSV (`sic_file` / `build_reference_classifications.py --sic-file`) alongside the live SEC fetcher. The SIC/Fama-French/NAICS taxonomy trees are now populated on the live DB (3 taxonomies, 125 nodes, 256 crosswalk rows); `entity_classification` stays empty until a SIC source is supplied for the universe.
- S8b adds a second offline SIC source: a SEC bulk `submissions.zip` fetcher (`EntityClassificationOptions.submissions_zip` / `build_reference_classifications.py --submissions-zip`) with lazy per-CIK lookup, so the full universe can be classified offline once the (one-time) bulk archive is downloaded — no network in tests.
- S9a adds the Compustat/FactSet **calculated-items / financial-ratio** surface: `fundamental_ratios` (`db/fundamental_ratios.py`, migration `0032`) is a long-format PIT fact (one row per `security_id,ratio_code,basis,period_end`) derived purely from `fundamental_ttm_points` (TTM flows) over `fundamental_statement_points` (instant balances) — no network, fully deterministic. v1 ships 15 ratios (margins, ROA/ROE, leverage, FCF/cash-flow, payout, book-value-per-share). Each ratio carries its **own** `available_at` = max availability of its specific inputs (true per-fact PIT), a `numerator_value`/`denominator_value` lineage pair, and an `is_meaningful` flag (false on non-positive denominators). As-of reader `fundamental_ratios_asof` + `query_asof --view fundamental-ratios`; DAG node depends on `sec_company_facts`. Live build materialized real rows over 5 securities (AAPL TTM @2026-03-28 net_profit_margin=0.2715, ROE=1.151, free_cash_flow=129.174B). v1 stores only the latest-revision input vintage (one ratio per period); restatement-vintage history is a planned non-breaking refinement.
- S9b expands the same engine to **23 ratios** (no schema change) — adds an `efficiency`/activity family (`asset_turnover`, `equity_turnover`) plus cash-flow-coverage and reinvestment ratios (`operating_return_on_assets`, `operating_cash_flow_margin`, `operating_cash_flow_to_assets`, `operating_cash_flow_to_liabilities`, `capex_to_operating_cash_flow`, `retention_ratio`), all from the existing 15 canonical metrics. Live rebuild grew `fundamental_ratios` to 5,029 rows / 23 codes.
- S9c adds a `growth` family (5 YoY ratios: revenue/net_income/operating_income/operating_cash_flow/assets `_growth_yoy`) via a pure-pandas prior-year pairing (`_attach_prior_year`, 350–380-day window); `ratio_kind='growth'`, value = (current − prior)/|prior|, not-meaningful off a negative base. Live rebuild grew `fundamental_ratios` to 6,468 rows / 28 ratio codes (1,439 growth rows).
- S9d adds average-balance returns (`average_return_on_assets`, `average_return_on_equity`, `operating_cash_flow_to_average_assets` — denominator = mean of ending + prior-year balance, the Compustat return-on-capital convention) plus `equity_growth_yoy`, reusing the prior-year pairing.
- S10a opens fundamental concept coverage **offline**: `fundamental_xbrl_metric` (`db/fundamental_xbrl_metrics.py`, migration `0033`) extracts the consolidated (zero-dimension-member context) us-gaap balance concepts the companyfacts feed omits, straight from the cached `xbrl_filing_facts` — no network. Maps current_assets/current_liabilities/cash_and_equivalents/inventory (bitemporal, restatement vintages). The ratio loader's new `balx` pivot unlocks a `liquidity` family (current/quick/cash ratios, working_capital via a new `difference` kind, working_capital_to_assets). Live: `fundamental_xbrl_metric=66`, `fundamental_ratios=7,793` over **37** ratio codes across **8 families** (AAPL @2026-03-28 current_ratio=1.0704, quick_ratio=1.0202, cash_ratio=0.3385). As-of reader `fundamental_xbrl_metric_asof` + `query_asof --view fundamental-xbrl-metric`.
- S10b extends the offline extractor to `LongTermDebt → long_term_debt` and adds a debt/solvency slice to the `leverage` family (`long_term_debt_to_equity`, `long_term_debt_to_assets`, `net_debt` via the `difference` kind, `net_debt_to_assets`).
- S10c extends the extractor to **annual (≈365-day) duration flow concepts** (gross_profit, cost_of_revenue, interest_expense, depreciation_amortization) and adds margin/coverage/EBITDA ratios (`gross_margin`, `cost_of_revenue_to_revenue`, `interest_coverage`, `ebitda` [level], `ebitda_margin`) that emit at fiscal-year period_ends.
- S10d adds a **financial-health** family: a `RetainedEarnings` extract feeds `retained_earnings_to_assets`/`equity_to_liabilities`, and a new `score` ratio_kind (with a `composite` callable on `RatioDef`) carries the **Altman Z″** distress score (book-equity variant, no price needed). Live: Altman Z″ ranks BA=1.29 (grey/stress), AAPL=3.37, ADBE=7.35 (safe).
- S10e adds the **Piotroski F-score** (`piotroski_f_score`, a second `score`-kind composite in the `health` family): nine YoY binary signals summed 0–9. The eight delta signals reuse `_attach_prior_year` (extended to long_term_debt/current_assets/current_liabilities/gross_profit + a new period-end shares metric); the share-issuance signal uses a new `CommonStockSharesOutstanding → common_shares_outstanding` XBRL **instant** extract (the dei cover-page share count in `fundamental_statement_points` is filing-dated, not period-end, so it never pairs YoY). Emits at fiscal-year period_ends with a complete prior year. Live: **AAPL FY2025 F=8** (high quality), **BA FY2025 F=7** (recovering).
- S10f adds the **Ohlson O-score** (`ohlson_o_score`, a third `score`-kind composite in the `health` family): the 1980 nine-term bankruptcy-probability logit. **No new XBRL concept** — needs only current balances/flows + the already-paired `ni_prior`, so it emits more broadly than the F-score (no annual-gross-profit dependency). Live: **10** Ohlson rows across AAPL/ADBE/BA, modeled P(default) AAPL/ADBE≈0.000, BA≈0.001.
- S10g adds three `efficiency`/activity ratios from two new instant concepts (`PropertyPlantAndEquipmentNet → property_plant_equipment_net`, `AccountsReceivableNetCurrent → accounts_receivable`): `fixed_asset_turnover`, `receivables_turnover`, `ppe_to_assets` (capital intensity). The derived surface then spanned **53 ratios + 3 composite distress scores across 9 families**. Live after S10g: `fundamental_xbrl_metric=199`, `fundamental_ratios=8,208`; cross-section captures business models — `ppe_to_assets` AMZN=0.436 (capital-heavy) vs ADBE=0.062 (asset-light) vs BAC=0.004 (bank).
- **S11 — first derived layer on the short-interest domain (Domain 9), and the broadest real surface in the warehouse.** `short_interest_metrics` (migration `0034`, `db/short_interest_metrics.py`) turns the cached FINRA feed into a typed PIT analytics surface, one row per (security_id, settlement_date): `short_interest_change`, `short_interest_change_pct`, recomputed `days_to_cover` (reconciled vs FINRA `days_to_cover_source`), `short_pct_shares_outstanding` (PIT shares join), plus the value-add the raw feed lacks — the **cross-sectional percentile** of days-to-cover / SI-change within each settlement cohort (the short-interest anomaly). PIT-safe (percentiles within one settlement cohort; `available_at`=publication time). Full house wiring (`short_interest_metrics_asof` + `query_asof --view short-interest-metrics`, jobs DAG ← `finra_short_interest`/`shares_outstanding_history`, 2 quality checks, lake, watermarks, build script). **Live: 62,054 rows over 21,576 securities / 204 settlements.** This breaks the 5–6-security fundamentals data ceiling — the FINRA feed covers the whole tape.
- S12 (migration `0035`, ALTER-only) extends `short_interest_metrics` with the tradeable time-series + tail form: `days_to_cover_zscore` (within-cohort standardized days-to-cover, population std), `days_to_cover_prev`/`days_to_cover_change` (per-security days-to-cover trend over the bi-monthly cycle). Live: 53,390 z-scores + 33,742 trend rows.
- **S13 — first derived layer on the macro domain (Domain 10), the second broad typed surface.** `macro_metrics` (migration `0036`, `db/macro_metrics.py`) turns the cached FRED feed into a typed PIT analytics surface, one row per (series_id, observation_date): `change_abs`, `change_yoy`/`yoy_growth` (CPI yoy_growth = inflation rate), expanding `zscore`, plus a synthetic `T10Y2Y` term spread (DGS10−DGS2, the yield-curve recession signal). Full house wiring (`macro_metrics_asof` + `query_asof --view macro-metrics`, jobs DAG ← `fred_macro`, 2 quality checks, lake, watermarks, build script). **Live: 53,102 rows over 7 series**; CPI inflation 4.17% (z=2.32), term spread +0.31. Macro-revision PIT is approximate (latest-revision FRED, not ALFRED).
- **S14 — first derived layer on the pricing domain (Domain 6).** `equity_price_metrics` (migration `0037`, `db/equity_price_metrics.py`) turns the cached daily bars into a typed PIT price-feature surface, one row per (security_id, trade_date): split/dividend-adjusted daily/log return, overnight gap, trailing realized vol (20d/60d), momentum (21d/126d), distance from trailing 252-day high, dollar volume. Computes its own **back-adjusted close from `split_factor`** because the sample's `adjusted_close` column is an unadjusted lagged close (would leave split jumps in). Full house wiring (`equity_price_metrics_asof` + `query_asof --view equity-price-metrics`, jobs DAG ← `daily_bars`, 2 quality checks, lake, watermarks, build script). **Live: 9,834 rows over 40 securities** (2012–13 sample). Insider Form-4 analytics deferred — cached `insider_transaction` is AAPL-only (data-starved).
- S15 (migration `0038`, ALTER-only) extends `short_interest_metrics` with multi-settlement momentum (`short_interest_momentum_3`, `days_to_cover_change_3`) and `is_squeeze_candidate` (top-decile crowding + rising days-to-cover). S23 (migration `0046`, ALTER-only) adds 6-settlement momentum/change, short-interest and days-to-cover acceleration, a bounded `short_pressure_score`, and `is_persistent_short_pressure`. S24 (migration `0047`, ALTER-only) adds liquidity-aware diagnostics: `average_daily_volume_percentile`, 99th-percentile winsorized days-to-cover + z-score, `liquid_short_pressure_score`, and `is_liquid_short_pressure`. Live after S24: 62,054 rows; winsorized days-to-cover populates 53,597 rows, liquid scores populate 17,964 rows, and there are 121 historical liquid-pressure rows.
- **S16 — first manager-side derived layer on the ownership domain (Domain 3).** `thirteenf_position_metrics` (migration `0039`, `db/thirteenf_position_metrics.py`) turns the cached SEC Form 13F feed (`thirteenf_security_positions` + `thirteenf_manager_reports`) into a typed PIT conviction-flow surface, one row per (manager_id, security_id, report_period): QoQ common-share change, `position_action` ∈ NEW/ADDED/TRIMMED/UNCHANGED/EXITED (exits inferred from the filed-quarter set; synthetic zero-share rows), and `voting_sole_pct`. Complements the issuer-side `thirteenf_security_ownership` rollup (built by `db/ownership.py`). Full house wiring (`thirteenf_position_metrics_asof` + `query_asof --view thirteenf-position-metrics`, jobs DAG ← `sec_13f_ownership_features`, 2 quality checks, lake, watermarks, build script). **Live: 6,595 rows over 6,032 managers / 54 quarters** (6,060 NEW / 228 TRIMMED / 205 ADDED / 94 UNCHANGED / 8 EXITED). PIT-safe (each value uses only the manager's current + immediately-prior-quarter filings). **Caveat:** the cached feed resolves all positions to one issuer (AAPL), so this is institutional ownership *of Apple* — broad on the manager axis, thin on the issuer axis; NEW dominates because pre-2026 consecutive-quarter coverage is sparse (the 392 continuations + 8 exits are the real flow signal, and both deepen as more 13F history/issuers load). Availability is approximate (warehouse ingest instant, never earlier than the true SEC filing).
- S17 (migration `0040`, ALTER-only) extends `macro_metrics` with `expanding_pct_rank` — where each observation sits within its series' own expanding history (0, 1]; the canonical macro regime signal ("VIX in its 95th percentile"). PIT-safe (expanding). Live rebuild repopulated all 53,102 rows; latest regime percentiles are coherent (UNRATE 0.257 low, VIXCLS 0.571, yields ~0.4 below historical median, CPI level 1.0 as expected for a monotonic index — use the percentile on mean-reverting series, not trending ones).
- Full DB test suite passed after S5k–S17: `python -m pytest db\tests` (**340** passed).
- Live default DB is migrated through `0040`; live `thirteenf_position_metrics=6,595`, `equity_price_metrics=9,834`, `macro_metrics=53,102`, `short_interest_metrics=62,054`, `fundamental_ratios=8,208`, `fundamental_xbrl_metric=199`. **Six typed PIT analytics surfaces now span fundamentals/ratios, short-interest, macro, pricing, and ownership.** Estimate counts after S5k smoke are `est_actual=1,240`, `est_surprise=1,222`, and `est_detail`/`est_consensus`/`est_guidance`/`est_recommendation`/`est_recommendation_summary`/`est_security_link=0` by design until licensed/manual files, accepted crosswalk evidence, or local SEC guidance text files are injected.

- **S18/S19/S20/S21/S22/S23/S24 update superseding the S17 summary above.** `corporate_action_dividend_metrics` (migration `0041`) adds cash-dividend spot/TTM yield and dividend-growth analytics with split-artifact filtering; live rows: **140** over 36 securities. `corporate_action_split_metrics` (migration `0042`) adds split-event reconciliation; live rows: **2** (KO and NKE), both `RECONCILED`. `thirteenf_option_metrics` (migration `0043`) adds issuer/report-period 13F call/put positioning aggregates; live rows: **22** AAPL issuer-period rows (18 `CALL_HEAVY`, 4 `PUT_HEAVY`) through 2026-03-31. `corporate_action_factor_reconciliation` (migration `0044`) adds event-level split/dividend factor reconciliation against adjacent `daily_adjustment_factors`; live rows: **142** (140 cash dividends + 2 splits), all `RECONCILED`. `thirteenf_concentration_metrics` (migration `0045`) adds issuer/report-period holder concentration analytics; live rows: **54** AAPL issuer-period rows through 2026-03-31, with latest `holder_count=6013`, `value_hhi=0.061111`, `top_5_holder_value_pct=0.439267`, and `concentration_bucket=MODERATE`. `short_interest_metrics` migrations `0046`/`0047` add 6-settlement momentum/acceleration, pressure scores, persistent-pressure flags, winsorized days-to-cover, liquidity percentile, and tradeability-gated pressure flags; live rows remain **62,054** with latest settlement 2026-06-15, 779 squeeze candidates, 11 liquid-pressure names, and max latest liquid score 85.4161. Latest S24 verification passed `py_compile`, focused short-interest/migration/job/quality/import tests, live as-of smoke, watermarks, warehouse quality, and lake export validation; warehouse quality ran 262 checks with only the same 5 standing failures. Live DB is migrated through `0047`; eleven typed PIT analytics/control surfaces now span fundamentals/ratios, short-interest, macro, pricing, ownership, and corporate actions.

- **S25 macro update superseding the older macro counts above.** `macro_metrics` now includes a second synthetic macro series, `REAL_FEDFUNDS`, computed as FEDFUNDS minus CPI YoY inflation in percentage points. Migration `0048` updates the macro catalog/PIT notes, and the new quality check fails if FEDFUNDS+CPI inputs with CPI YoY exist but the synthetic row is missing. Live build run `94a6d1d0-951c-470d-ad08-9566988407b6` materialized **53,964 rows over 8 series** (including 862 `REAL_FEDFUNDS` rows); latest `REAL_FEDFUNDS` is 2026-05-01 at -0.5366146840 percentage points with `available_at=2026-05-01 23:00:00`. Warehouse quality ran 263 checks with only the same 5 standing failures. Lake export run `3c2733b1-f25f-4b96-9d8a-f5fa201fa7d0` validated 1 file / 53,964 rows / 0 problems. Live DB is migrated through `0048`. Caveat remains: macro-revision PIT is approximate until ALFRED vintages replace/latest-augment the current latest-revision FRED cache.

- **S26 off-exchange/short-interest update.** `finra_short_volume` and `short_volume_metrics` (migration `0049`, `db/short_volume.py`, `scripts/build_short_volume.py`) add an injectable FINRA daily short-volume flow slice. The raw table stores FINRA market-code rows by symbol/date/market with source-file lineage, `available_at`, `is_latest`, and restatement sequence; the derived table rolls to one row per symbol/date with `short_volume_ratio`, `short_exempt_ratio`, dominant market code/share, same-day cross-sectional percentiles, and `is_high_short_flow`. As-of APIs/CLI, jobs, lake exports, watermarks, and 5 quality checks are wired. The two S26 tables are **empty by design** until real FINRA daily files are injected; isolated smoke loaded 3 fixture rows -> 2 metric rows and validated lake export.

- **S27 fundamentals health-score update.** `fundamental_xbrl_metric` now maps combined annual SG&A (`SellingGeneralAndAdministrativeExpense(s) → selling_general_and_administrative_expense`) and `fundamental_ratios` adds the **Beneish M-score** (`beneish_m_score`) as the fourth `health`/`score` composite. It uses DSRI, GMI, AQI, SGI, DEPI, SGAI, TATA, and LVGI with the existing per-input `available_at` max and `_attach_prior_year` PIT pairing. No schema migration was needed. Live no-initialize rebuilds materialized `fundamental_xbrl_metric=202` under run `8a4d2cee-e4a4-4398-82b4-d224502386aa` and `fundamental_ratios=8,209` / **54 ratio codes** under run `96fe0930-de96-495f-918e-7055b9c6a774`; one AAPL FY2025 Beneish row emitted (`value=-2.293696`, `available_at=2026-05-01 22:00:00`). Lake export run `a0a08503-88ba-4f46-922d-4e668fdea083` validated 2 files / 8,411 rows / 0 problems; no S27 fundamental-ratio/XBRL regression.

- **S28 off-exchange quality-control update.** `offexchange_quality_report` (migration `0050`, `db/offexchange_quality.py`, `scripts/build_offexchange_quality_report.py`) materializes period-level control reports over both public off-exchange surfaces: OTC transparency (`offexchange_volume`) and FINRA daily short-volume (`finra_short_volume`/`short_volume_metrics`). It records row/security/venue-market counts, volume totals, ATS share, short-volume ratio, high short-flow counts, restatement/multiple-latest counts, bad-row/missing-availability counts, max publication lag, source-input JSON, `available_at`, and report revisions. As-of API/CLI, jobs DAG, lake export, watermarks, and 3 quality checks are wired. Shared DB is migrated through `0050`; the report table is **empty by design** until approved FINRA files are injected. Isolated smoke materialized 2 report rows and exported the lake object. Shared quality now runs 271 checks with only the same 5 standing failures.

- **S29 insider transaction signal update.** `insider_transaction_metrics` (migration `0051`, `db/insider_metrics.py`, `scripts/build_insider_transaction_metrics.py`) materializes issuer-day/trailing-window Section 16 signals from loaded `insider_transaction` rows: open-market purchases/sales, plan-vs-discretionary sale split, 10b5-1-heavy-sale flag, cluster-buy counts/value, discretionary sell-pressure, source transaction lineage JSON, and late-filing revisions by `available_at`. As-of API/CLI, jobs DAG, lake export, watermarks, and 3 quality checks are wired. Shared DB is migrated through `0051` and currently has 13 derived rows from the loaded Section 16 sample; shared quality runs 274 checks with the same 5 standing failures.

- **S30 Form 144 reconciliation update.** `form144_intent` and `form144_to_form4_link` (migration `0052`, `db/form144.py`, `scripts/build_form144.py`) add an offline/injectable SEC Form 144 intent-to-sell surface plus conservative links to visible Form 4 sale executions. Intent rows carry source-file hashes, seller CIK/name normalization, proposed shares/value/price, sale windows, 10b5-1 plan fields, latest/restatement markers, `as_of_date`, and `available_at`; link rows carry match method/confidence/status, share/value completion, and later-of-source availability. As-of API/CLI, jobs DAG after `sec_insider_ownership`, lake export, watermarks, and Form 144 quality checks are wired. Shared DB is migrated through `0052`; shared Form 144 tables are currently empty until source files are injected; shared quality runs 279 checks with the same 5 standing failures.

- **S37 market-relative price risk factors (EW beta + idiosyncratic volatility).** Extended `equity_price_metrics` (migration `0058`, ALTER-only) with `market_return_ew`, `beta_60d`, `market_correlation_60d`, and `idiosyncratic_vol_60d`. The market proxy is the same-day equal-weight return across the loaded bar universe, and row `available_at` is delayed to the latest same-day bar used in that proxy. Live: 9,834 rows; 9,794 populated market-return rows; 7,434 populated beta/correlation/idiosyncratic-vol rows; AAPL latest beta=0.8939, corr=0.2353, idio vol=0.3692. Quality 283 checks, standing failures still 2; lake export validated 1 file / 9,834 rows / 0 problems. Caveat: this is an internal cached-sample EW proxy, not an official CRSP/SPY benchmark.

- **S36 price risk factors (max drawdown + downside deviation).** Extended `equity_price_metrics` (migration `0057`, ALTER-only) with `max_drawdown_126d` (trailing-126d most-negative price/expanding-peak − 1; ≤0) and `downside_deviation_60d` (annualized Sortino-denominator downside semi-deviation, MAR=0). Bars-only, PIT-safe. Live: 4,834 populated drawdown rows (AAPL=-0.3962, capturing its 2012-13 drawdown), 7,434 downside-dev rows. Quality 283 checks, standing failures still 2. Coverage capped at the 40-security 2012-13 price sample.

- **S35 price liquidity factors (ADV + Amihud illiquidity).** Extended `equity_price_metrics` (migration `0056`, ALTER-only) with `avg_dollar_volume_21d` (trailing 21-day average daily dollar volume — a capacity measure) and `amihud_illiquidity_21d` (Amihud 2002 illiquidity = trailing 21-day mean of |daily_return|/dollar_volume, scaled ×1e9 = price impact per $1B traded). Both are backward-looking, PIT-safe, bars-only. Live: 9,834 rows, 8,994 with populated Amihud (post 21-day warmup); cross-section sensible (ADV $100M–$384M, amihud 0.03–0.08). Quality 283 checks, standing failures still 2. Coverage capped at the cached 40-security 2012-13 price sample; scales when overlapping price history loads.

- **S34 macro Sahm Rule recession indicator.** Added a third synthetic macro series `SAHM_RULE` to `macro_metrics` (`db/macro_metrics.py` `_append_sahm_rule`): the 3-month UNRATE moving average minus its trailing-12-month minimum (≥0.5pp = recession onset; Sahm 2019 / FRED `SAHMREALTIME`). Pure offline derivation from cached UNRATE, PIT-safe. Migration `0055` catalogs it; a new `missing_sahm_rule_when_unrate_available` quality check guards it. Live: macro_metrics **54,891** rows / **9 series**, 927 monthly SAHM rows (1950–2026); latest 2026-05 = 0.10, historical max 2020-06 = 9.43 (COVID, correctly detected). Quality 283 checks, standing failures still 2. Remaining macro extensions need network (DGS3MO for 3m10y spread, ALFRED vintages for exact revision PIT).

- **S33 statement-map quality-check fix (fundamentals).** Cleared the standing `bad_fundamental_statement_map_rows` (37) failure. The flagged rows were the legitimate S4 bank/insurance/REIT overlay map rows (`statement_type` ∈ `bank_statement`/`insurance_statement`/`reit_statement`, plus a `quantity` `unit_type` for `gross_leasable_area`); the bad-row check allowlists in `db/quality.py` (3 statement_type/category sites + the unit_type site) were stale. Extended the allowlists; data unchanged, code-only (no migration). Live: warehouse quality standing failures **3 → 2** (remaining: XBRL calculation-linkbase 1,364 — real as-reported data quirks; provider-parity-without-open-tables 8 — honest empty injectable surfaces).

- **S32 reference-data integrity repair (security identifier history).** Fixed two long-standing quality failures (`duplicate_identifier_history_keys`, `identifier_same_source_self_overlaps`). Root cause: the SEC ownership issuer seed (`db/insider_ownership.py`) wrote one open-ended `security_identifier_history` row per Form 4 filing, so an issuer observed across N filings accumulated N overlapping CIK/TICKER intervals. `db/security_master.py` adds `dedupe_open_identifier_intervals` (collapse repeated open-ended rows per `(security_id, id_type, id_value, source)` to the earliest `valid_from`) and `collapse_identifier_history_open_duplicates` (table-wide repair); the issuer-seed upsert is now idempotent (dedupe batch + insert only when no open interval exists); migration `0054` runs the one-time repair. Live: identifier rows 32,734 → 32,686, `self_overlaps` 528 → 0, `duplicate_keys` 10 → 0; warehouse quality standing failures **5 → 3** (remaining: XBRL calculation-linkbase 1,364, fundamental statement-map 37, provider-parity-without-open-tables 8). Genuine closed intervals (ticker changes) are untouched.

- **S31 reference/listing analytics update — first derived layer on Domain 5 (reference/listing), and a broad real surface.** `security_listing_metrics` (migration `0053`, `db/listing_metrics.py`, `scripts/build_security_listing_metrics.py`) joins the CIK-resolved `listing_status_intervals` spine to the cached Nasdaq Trader symbol directory (`nasdaq_symbol_directory`) and emits one PIT reference-compliance row per (security_id, snapshot as_of_date). It decodes the Nasdaq `financial_status` code (N/D/E/Q and combined G/H/J/K) into typed `is_deficient` / `is_delinquent` / `is_bankrupt` / `is_noncompliant` / `is_listing_compliant` flags — a delisting-risk / financial-distress signal across the whole listed tape — plus market-tier (Global Select / Global Market / Capital Market), exchange-name, ETF, test-issue, next-shares, and round-lot classification. Availability is the directory snapshot ingest instant; a `financial_status` flip between daily snapshots becomes a new later-dated row (restatement vintages preserved). Full house wiring (`security_listing_metrics_asof` + `query_asof --view security-listing-metrics`, jobs DAG ← `listing_status_intervals`, 3 quality checks, watermarks, lake export, FactSet+CRSP security_master parity rows). **Live: 12,026 rows over 6,013 securities across 2 daily snapshots (2026-06-27/28); latest-snapshot compliance N=6,194, otherlisted/none=5,198, D=564, E=40, H=30 — 634 non-compliant delisting-risk names; 216 ETFs.** This breaks the 5–6-security fundamentals data ceiling on the reference domain the same way S11 did for short-interest. Shared DB migrated through `0053`; shared quality runs **282** checks with the same 5 standing failures. Caveats: `financial_status` is nasdaqlisted-only (NYSE/Arca names carry `has_financial_status=false` = unknown, not non-compliant); coverage is capped at securities the listing spine resolves to a `security_id` (~6,013); availability is approximate (warehouse ingest instant).

## Important Caveat

Default DB estimate detail/consensus/guidance/recommendation/summary/security-link rows remain empty unless a licensed/manual file, local SEC guidance corpus, or accepted estimate crosswalk is injected. The schema, loaders, as-of APIs, watermarks, lake export, and quality checks are ready, but no real IBES/FactSet/CIQ/Zacks files or SEC exhibit text corpora have been loaded into the shared DB. Estimate as-of APIs resolve vendor-keyed rows through `est_security_link` only when the link's own `available_at`, `as_of_date`, and validity window are visible to the query.

During S5g live smoke, an initial DuckDB migration shape triggered a DuckDB internal error while adding indexes in the same transaction as `ALTER TABLE`. The corrected implementation splits schema migration `0017` and index migration `0018`. The failed WAL was preserved as `db/atx_impl.duckdb.wal.failed-s5g-migration.20260629-074343.bak`, and the DB checkpoint was backed up as `db/atx_impl.duckdb.pre-s5g-wal-recovery.20260629-074343.bak` before moving the WAL aside.

During S5k live smoke, the same DuckDB `ALTER TABLE` + index transaction shape recurred for `est_guidance`. The corrected implementation splits guidance extraction columns/catalog migration `0027` and index migration `0028`. The failed WAL was preserved as `db/atx_impl.duckdb.wal.failed-s5k-migration.20260629-094756.bak`, and the DB checkpoint was backed up as `db/atx_impl.duckdb.pre-s5k-wal-recovery.20260629-094756.bak` before moving the WAL aside.

## Key Files

- Schema/migrations/catalog: `db/schema.py`, `db/migrations.py`, `db/parity.py`, `db/PARITY_GAP.md`
- S4 fundamentals: `db/fundamental_statements.py`, `db/xbrl_validation.py`
- S5/S18-S21 share counts / adjustment factors / delistings / estimates / corporate-action controls: `db/shares_outstanding.py`, `db/adjustment_factors.py`, `db/corporate_action_metrics.py`, `db/delisting.py`, `db/estimates.py`, `db/estimate_security_links.py`, plus `db/asof.py`, `db/jobs.py`, `db/watermarks.py`, `db/quality.py`
- S9-S10/S27 fundamentals ratio/score surfaces: `db/fundamental_ratios.py`, `db/fundamental_xbrl_metrics.py`, `scripts/build_fundamental_ratios.py`, `scripts/build_fundamental_xbrl_metrics.py`
- S11-S12/S15/S23-S24/S26/S28 short-interest, daily short-flow, and off-exchange quality metrics: `db/short_interest_metrics.py`, `db/short_volume.py`, `db/offexchange_quality.py`, `scripts/build_short_interest_metrics.py`, `scripts/build_short_volume.py`, `scripts/build_offexchange_quality_report.py`
- S16/S20/S22/S29/S30 ownership metrics: `db/thirteenf_position_metrics.py`, `db/thirteenf_option_metrics.py`, `db/thirteenf_concentration_metrics.py`, `db/insider_metrics.py`, `db/form144.py`, `scripts/build_insider_transaction_metrics.py`, `scripts/build_form144.py`
- S31 reference/listing metrics: `db/listing_metrics.py`, `scripts/build_security_listing_metrics.py` (derives `security_listing_metrics` from `listing_status_intervals` + `nasdaq_symbol_directory`)
- Scripts: `scripts/build_quant_warehouse.py`, `scripts/query_asof.py`
- Tests added/updated across recent tranches: `db/tests/test_shares_outstanding_history.py`, `db/tests/test_adjustment_factors.py`, `db/tests/test_daily_adjustments.py`, `db/tests/test_delisting.py`, `db/tests/test_estimates.py`, `db/tests/test_xbrl_validation.py`, `db/tests/test_fundamental_period_dates.py`, import/migration/job/quality tests

## Recommended Next Slice

0-SI. **Build out the broad-coverage typed surfaces — these break the fundamentals data ceiling (TOP PRIORITY).** The S9–S10 ratio/score work is comprehensive but capped at the **5–6 securities** in the offline `xbrl_filing_facts` cache. S11 showed the way out: the FINRA short-interest feed covers **~21.6k securities** offline, so a typed derived surface over it (`short_interest_metrics`, 62k rows) is the broadest real surface in the warehouse. Continue mining the broad cached feeds the same way:
   - **(i) Short-interest analytics — DONE through S28 for typed level/trend/momentum/acceleration/liquidity pressure, injectable daily short-volume flow, and off-exchange quality controls.** `short_interest_metrics` carries recomputed days-to-cover, PIT short-%-shares, cohort percentile/z-score, 1-/3-/6-settlement trend and momentum, acceleration, squeeze/persistent/liquid-pressure flags, bounded pressure score, ADV percentile, and winsorized days-to-cover. S26 adds `finra_short_volume` + `short_volume_metrics` for FINRA daily short-volume flow proxy rows and per-symbol/day short-flow ratios/percentiles. S28 adds `offexchange_quality_report` to summarize OTC transparency and daily short-flow coverage, restatements, bad rows, publication lag, and lineage. Remaining value: live FINRA daily-file/backfill connector, FINRA OTC Transparency connector, and borrow-fee/utilization only if licensed.
   - **(ii) Macro analytics — DONE in S13 (`macro_metrics`, 53,102 rows / 7 series).** change/YoY/expanding-z per series + synthetic `T10Y2Y` term spread. Remaining macro extensions: real fed funds (FEDFUNDS − CPI `yoy_growth`, a monthly cross-frequency join), 3m10y spread, VIX regime percentile, World Bank/IMF series; ALFRED vintage ingestion would upgrade macro-revision PIT from approximate to exact (network-gated).
   - **(ii-a) Macro analytics S25 update.** Real fed funds is now DONE as `REAL_FEDFUNDS` (FEDFUNDS minus CPI YoY inflation), with 53,964 live rows over 8 series and migration `0048`. Remaining macro extensions: 3m10y spread (needs DGS3MO cached), World Bank/IMF public series, and ALFRED vintage ingestion for exact macro-revision PIT. VIX regime percentiles are already covered by S17 `expanding_pct_rank`.
   - **(iii) Corp-action-adjusted return/control surfaces — DONE in S18-S21 for the cached overlap.** Dividend analytics, split-event reconciliation, and all-event factor reconciliation now join `corporate_actions`/`adjustment_factor_history` to `equity_daily_bars`/`daily_adjustment_factors`. Remaining value is broader recent overlapping event/bar history plus spinoff, merger, Form 8937 basis allocation, and delisting-return evidence.
   - **(iv) Insider (Form-4/Form-144) analytics — S29 signal and S30 reconciliation surfaces built, breadth still thin.** `insider_transaction_metrics` now derives PIT-safe open-market purchase/sale, 10b5-1 plan-sale contamination, cluster-buy, and discretionary sell-pressure rows from loaded `insider_transaction` records. `form144_intent` and `form144_to_form4_link` now reconcile injectable Form 144 sale notices to visible Form 4 sale executions. Remaining value is broad Form 3/4/5 backfill, populated Form 144 source coverage, pre-2023 10b5-1 inference, N-PORT/N-PX/congressional disclosure surfaces, and household/entity rollups; keep network/API calls out of tests.
   - **(v) Ownership analytics — DONE in S16/S20/S22 for the cached feed.** `thirteenf_position_metrics` has 6,595 manager/security/period flow rows over 6,032 managers; `thirteenf_option_metrics` has 22 AAPL issuer-period call/put positioning rows; `thirteenf_concentration_metrics` has 54 AAPL issuer-period holder-concentration rows with HHI/top-holder/effective-holder-count metrics. Remaining ownership extensions: manager portfolio-turnover once the universe widens beyond the single cached issuer (AAPL), curated subadvisor/M&A continuity seeds, and N-PORT adviser-to-13F cross-links.

00. **Pick the next fundamentals slice (all offline, real rows, build on S9–S10g/S27).** Capped at 5–6 securities — see 0-SI for why broad feeds beat more fundamental ratios. The derived-ratio surface now spans **54 ratio codes / 9 families**, including four `health` composites (Altman Z'', Piotroski F, Ohlson O, Beneish M), over an offline XBRL extractor (`db/fundamental_xbrl_metrics.py` → `fundamental_xbrl_metric`, instant + annual-duration). Highest-value continuations, in rough priority:
   - **(a) Quarterly-TTM stitching of the flow concepts — DEPRIORITIZED on the current cache (feasibility checked S10g).** The intent was to make S10c's margin/coverage/EBITDA ratios (and the S10e Piotroski gross-margin signal) quarterly by reconstructing a trailing-twelve-month rollup (10-Qs report 3-mo directly; Q4 3-mo = annual − 9-mo YTD; sum trailing four). **Blocker found:** the cached consolidated duration windows for the flow concepts are **3-mo / 6-mo / 12-mo only — there is NO ~9-mo (YTD) window** (histogram over `GrossProfit`/`CostOfGoodsAndServicesSold`/`Revenues`: ~3mo and ~6mo and ~12mo buckets populated, ~9mo empty), and the ~3-mo facts are sparse (≤3 securities, ~10 facts/concept). So neither the annual−9mo reconstruction nor a direct trailing-four-quarter sum yields more than a handful of complete TTM windows — the effort does not pay off until the security universe is widened. `fundamental_statements.refresh_fundamental_ttm_points` remains the reference algorithm once richer 10-Q duration coverage exists. **Re-ranked:** the real next lever is **universe expansion** (more securities → more consecutive years → all the FY-gated ratios/scores and a viable quarterly stitch), which is network-gated (build/smoke only, never tests): widen the companyfacts/`xbrl_filing_facts` cache beyond the current 5–6 securities.
   - **(b) Composite scores DONE: Altman Z'' (S10d) + Piotroski F (S10e) + Ohlson O (S10f) + Beneish M (S27).** The `score`-kind composite mechanism is now proven for distress, quality, default-risk, and earnings-manipulation screens. Beneish live coverage is currently one AAPL FY2025 row because combined annual SG&A plus a complete prior fiscal year is the binding constraint; Piotroski is similarly FY-gated by annual `gross_profit`. Further score math is lower value than widening the XBRL/security universe, adding restatement-vintage ratio history, or loading overlapping price history for valuation multiples.
   - **(c) Price-based valuation multiples** — still blocked by the 2012–2013 price sample not overlapping 2017–2026 fundamentals (load overlapping price history first; see 0b below).
   - Verified-available cached concepts for (a)/(b): COGS 3 secs, GrossProfit 3, D&A 5, RetainedEarnings (check `xbrl_filing_facts` — `RetainedEarningsAccumulatedDeficit`).

   *(Historical alternative, now superseded by S10a's offline path — kept for reference: widening `DEFAULT_CONCEPTS` in `db/fundamentals.py` + re-fetching companyfacts is the network route to the same metrics; the cached `sec_company_facts` carries only 16 concepts.)* Two sources:
   - **(A, network) Widen `DEFAULT_CONCEPTS`** in `db/fundamentals.py` (AssetsCurrent, LiabilitiesCurrent, CashAndCashEquivalents, CostOfRevenue/CostOfGoodsAndServicesSold, GrossProfit, InventoryNet, accounts-receivable, InterestExpense, DepreciationDepletionAndAmortization, LongTermDebt/DebtCurrent, IncomeTaxExpenseBenefit), add the matching `fundamental_statement_map` rows, re-fetch companyfacts for the 5 securities (**SEC network — build/smoke only, never in tests**), then re-run `refresh_fundamental_statement_points` → `refresh_fundamental_ttm_points` → `build_fundamental_ratios`. Cleanest (companyfacts is pre-consolidated), but needs network.
   - **(B, OFFLINE — verified feasible) Extract from the already-cached `xbrl_filing_facts`** (1,079 distinct us-gaap numeric concepts incl. all the above; AssetsCurrent has 18 facts/4 secs, COGS/GrossProfit/Inventory/D&A/LongTermDebt all present). **Verified consolidated-total selection rule:** a fact is the entity-level (non-segment) value iff its `filing_context_id` has **zero rows in `xbrl_filing_dimensions`** (probe: 18/18 AssetsCurrent facts are undimensioned, 0 dimensioned). Recipe: `xbrl_filing_facts` (taxonomy='us-gaap', is_numeric) JOIN `xbrl_filing_contexts` (for `period_type`/`period_start`/`period_end`/`instant_date`) WHERE `filing_context_id NOT IN (SELECT filing_context_id FROM xbrl_filing_dimensions)`; dedup to the latest filing per (security, concept, period) by acceptance/`available_at`; map concept→`canonical_metric`; emit as new statement points (decide: extend `fundamental_statement_points` via a new loader, or a parallel `fundamental_xbrl_metrics` table that the ratio balance/flow pivots also read). **Caution — high blast radius:** `fundamental_statement_points`/`_ttm_points` feed features + ratios; build the new path with its own ids + revision handling and full TDD before wiring into the ratio loader. This is the single biggest core-ratio-parity unlock and is doable with no network.
0b. **Price-based valuation multiples (deferred — blocked by a data-coverage gap, NOT design).** P/E, P/B, P/S, EV/EBITDA, dividend/earnings/FCF yield would join market-cap (`equity_daily_bars` adjusted close × PIT `shares_outstanding_history`) to the fundamentals. BLOCKER discovered in S9b: cached `equity_daily_bars` is a **2012–2013** 40-security sample while fundamentals are **2017–2026** — zero temporal overlap, so a PIT price×fundamental join yields ~0 live rows. Load overlapping daily price history first (the price loader is `db/features.py`/`tbltickerhistory*`), then build the multiples (per-fact `available_at = max(price.available_at, fundamental.available_at)`; inject a tiny price+shares fixture in tests, never a price API). Reuse `compute_ratio_rows`-style pure math.
0. **Off-exchange + 13F follow-ups.** (a) Wire the live FINRA OTC Transparency connector for S7a/S28: FINRA Query API `otcMarket` group (OAuth2 client-credentials, token endpoint `https://ews.fip.finra.org/fip/rest/ews/oauth2/access_token`), `POST {base}/data/group/otcMarket/name/weeklySummary`, with the 10 GB/mo cap guard and tiered publication-delay period computation (Tier1 ~14d), feeding `scripts/build_offexchange.py` and then `scripts/build_offexchange_quality_report.py` — keep network calls out of the test path. (b) Wire an offline/live FINRA daily short-volume backfill downloader into `scripts/build_short_volume.py` and refresh S28 reports. (c) Load a curated subadvisor / M&A-continuity seed CSV into `filer_13f_cik_alias` via `python scripts/build_filer_aliases.py --seed-file <csv>` (columns: `parent_cik,child_cik,alias_type,valid_from,confidence,evidence[,valid_to,available_at]`) and verify `resolve_primary_cik` rolls children up PIT-correctly.
1. Load a small approved licensed/manual estimates sample through `--estimate-detail-file`, `--estimate-consensus-file`, `--estimate-recommendation-file`, and `--estimate-recommendation-summary-file`, plus accepted `identifier_resolution_decisions`, then verify `est_security_link` resolves vendor identifiers (`IBES ticker`, CUSIP, FactSet fsym, CIQ trading item) without leaking future links into earlier as-of queries.
2. Add broker-name alias joins where recommendation IDs do not match detail estimate IDs.
3. Feed an approved local SEC 8-K Item 2.02/7.01 exhibit corpus through `--estimate-guidance-file`, inspect false positives/negatives, then extend the parser for open-ended guidance, fiscal calendars, and additional measures without public API calls in tests.
4. Separately, clean up existing non-estimate quality failures. **DONE in S32:** duplicate identifier history keys and overlapping identifier intervals (root-cause fix in the issuer-seed loader + migration `0054` repair). **Remaining:** XBRL calculation-linkbase validation rows (1,364 — largely real as-reported data quirks; triage true errors vs. expected), fundamental statement-map gaps (37), and provider-parity rows without open tables (8 — honest empty injectable surfaces). The four table-catalog gaps were closed earlier (S7b).

## Verification Commands

Run from `C:\atx`:

```powershell
cd C:\atx\atx-impl
python -m pytest db\tests -q
```

Live smoke examples from `C:\atx\atx-impl`:

```powershell
python scripts\build_estimates.py --db-path db\atx_impl.duckdb --run-estimate-security-links
python scripts\build_estimates.py --db-path db\atx_impl.duckdb --estimate-guidance-file path\to\local_8k_guidance.csv
python scripts\query_asof.py --db-path db\atx_impl.duckdb --as-of-date 2026-06-29 --view estimate-security-links --limit 5
python scripts\query_asof.py --db-path db\atx_impl.duckdb --as-of-date 2026-06-29 --view estimate-guidance --limit 5
python scripts\query_asof.py --db-path db\atx_impl.duckdb --as-of-date 2026-06-29 --view estimate-consensus --limit 5
python scripts\query_asof.py --db-path db\atx_impl.duckdb --as-of-date 2026-06-29 --view estimate-recommendations --limit 5
python scripts\query_asof.py --db-path db\atx_impl.duckdb --as-of-date 2026-06-29 --view estimate-recommendation-summary --limit 5
python scripts\query_asof.py --db-path db\atx_impl.duckdb --as-of-date 2026-06-29 --view estimate-actuals --measure-codes EPS_DILUTED --limit 5
python scripts\query_asof.py --db-path db\atx_impl.duckdb --as-of-date 2026-06-30 --view insider-transaction-metrics --symbols AAPL --limit 5
python scripts\query_asof.py --db-path db\atx_impl.duckdb --as-of-date 2026-06-30 --view form144-intents --symbols AAPL --limit 5
python scripts\query_asof.py --db-path db\atx_impl.duckdb --as-of-date 2026-06-30 --view form144-reconciliation --symbols AAPL --limit 5
```

## Tranche Tracking

Use `WAREHOUSE_PARITY_TRANCHES.md` as the append-only implementation ledger. At the end of every coherent tranche, add a row with:

- start commit and end commit SHA
- files or domains touched
- tests and live DB smoke checks run
- known caveats and the next recommended tranche

Do not rewrite older rows except to fix factual mistakes. The point is to make long-running unattended work resumable without archaeology.

## North-Star Goal Prompt

Use `C:\atx\archive\research`, `C:\atx\atx-impl\db`, and `C:\atx\atx-impl\scripts` to build a feature-parity rival to FactSet and S&P Compustat for all US equity fundamental and public alternative data. Design it as a robust schema-based warehouse for quant equity long/short shops like Renaissance Technologies and WorldQuant.

The north star is full end-to-end replication quality for US equity fundamentals, ownership, corporate actions, pricing adjustments, reference data, estimates where public or injectable licensed feeds allow it, off-exchange/short-interest, macro, and other public alt-data surfaces. Prioritize production-quality infrastructure, explicit point-in-time semantics, versioned migrations, bitemporal availability fields, lineage, watermarks, lake exports, quality checks, as-of APIs, tests, and clear DB design over thin demos.

Work autonomously for hours when needed. Avoid stopping to ask questions unless continuing would risk destructive or clearly mis-scoped work. Prefer making conservative, well-documented assumptions, implementing complete vertical slices, validating with tests and live smoke checks, and recording progress in markdown tranche ledgers with commit SHAs. Use subagents when useful to preserve main-agent context, but keep write scopes clear. Do not touch C++ code unless the user explicitly redirects. Ensure testing suite stays light, fast, and avoids hitting public apis.
