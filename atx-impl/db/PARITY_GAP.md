# ats-eqt Parity Gap Matrix

**Purpose:** Table-by-table, domain-by-domain gap analysis driving the multi-sprint build toward FactSet / S&P-Compustat / Refinitiv parity on equity fundamentals + ownership + supply-chain.
**Target (blueprint):** `C:\atx\archive\research\` — `schemas/data_models_and_methodology.md` (consolidated DDL), `schemas/cross_vendor_field_map.md` (137 cross-industry + 37 overlay canonical fundamentals fields), `datasets/*.md` (per-dataset DDL), `sources/public_data_sources.md` (44+ upstream sources).
**Current (built):** `C:\atx\atx-impl\db\schema.py` plus migrations through `0179`, loader modules in `db/`, parity ledger seed `db/parity.py` (14 provider rows), the PF-S2 in-repo job orchestrator control plane, the PF2-S1 schema-as-contract / warehouse-catalog enforcement layer, the PF2-S2 migration-governance / backup-checkpoint-restore tooling, the PF2-S3 standardized fundamentals engine, the PF2-S4 PIT month snapshot / ratio-vintage surfaces, the PF2-S5 industry-template routing/coverage layer, the PF2-S6 calendarization/TTM normalization layer, the PF2-S7 segment/footnote sub-ledger layer, the PF2-S8 preliminary press-release / estimate-basis layer, the PF2-S9 valuation-overlap / cross-vendor reconciliation / expanded SQL-DQC layer, the PF2-S10 quality-gated production observability / storage / rebuild capstone, the PF3-S1 backfill and incremental-maintenance rails, the PF3-S2 schema-contract v2 close, the PF3-S3 architecture decomposition split, the PF3-S4 governed PIT universe plus price-backfill client surfaces, the PF3-S5 valuation-input completeness layer, the PF3-S10 backtest-ready factor-panel export boundary, and the PF4-S1 per-factor signal-evaluation surface (rank-IC/decay, quantile spread/turnover, correlation/crowding, breadth) plus gated leakage/coverage factor DQC, the PF4-S2 factor-panel orchestrator gate plus factor freshness/anomaly/lineage observability and codified maintenance schedule, and the PF4-S3 code-hardening close, which confirms migrations still run through `0183` (no migration added this sprint) and closed the S1-S10 review's one High plus five Medium and five Low findings with proof tests, re-greening and de-time-bombing the offline suite.
**Author:** read-only analysis started 2026-06-28; implementation status updates appended through PF4-S3.

Fill-level legend: **Built** = table exists + loader + non-trivial rows; **Partial** = table exists but missing key columns/sub-tables or thin coverage; **Stub** = schema present, ~0 rows / no real loader; **Missing** = no table at all.

---

## 1. Executive Summary

| # | Domain | Target tables (n) | Built | Partial | Missing | Current loader(s) | Sprint |
|---|--------|------:|-------|---------|---------|-------------------|:------:|
| 1 | Fundamentals (XBRL/us-gaap) | ~14 | sec_company_facts, fundamental_points, fundamental_statement_points, fundamental_ttm_points, fundamental_periods, fundamental_fact_revisions, xbrl_* + xbrl_validation_results, segment_dim, segment_fact, footnote_*, press_release_facts, press_release_reconciliation, vendor_baseline_facts, fact_disagreement | S4a/S4b statement map, rdq/four-date period fields, dimension-aware calc-linkbase validation, referential quality checks, expanded SQL-native DQC subset, `resolution_status`, calendarization/TTM normalization, dimensional segment/footnote mining, preliminary-to-final press-release reconciliation, and injectable cross-vendor standardized-fact disagreement tracking are built; full Arelle/DQC sidecar and live vendor baselines remain | full DQC 150+ execution, loaded Sharadar/SimFin baseline proof slice, live press-release proof-slice corpus/counts, operator live split for the legacy 1,364 calc rows, live segment/footnote proof-slice counts | fundamentals.py, fundamental_statements.py, xbrl_taxonomy.py, xbrl_filing_contexts.py, xbrl_validation.py, segments.py, footnotes.py, press_release.py, fact_disagreement.py, quality.py | 9 (polish) |
| 2 | Estimates (IBES/consensus) | ~11 | est_measure, est_actual, est_surprise, est_broker, est_broker_alias, est_analyst, est_analyst_alias, est_period_dim, est_detail, est_consensus, est_guidance, est_recommendation, est_recommendation_summary, est_security_link | SEC-derived actuals/SUE, actual basis tags, basis-aware surprise mismatch handling, local SEC 8-K guidance/press-release text extraction, injectable detail/consensus/recommendation landing surfaces, and PIT-safe vendor-security-id links | default DB has no licensed broker-detail/consensus/recommendation rows, accepted estimate crosswalks, or loaded SEC guidance / press-release corpus | build_estimates.py, estimates.py, press_release.py, estimate_security_links.py | 2 (extend) |
| 3 | Ownership-13F | 5 | thirteenf_managers, thirteenf_manager_reports, thirteenf_security_positions, thirteenf_security_ownership, filer_13f_cik_alias, thirteenf_position_metrics, thirteenf_option_metrics, thirteenf_concentration_metrics | S6a filer CIK-alias / entity-resolution spine built; S6b 2023-01-03 value-unit cutover normalized; S16 manager-level conviction-flow metrics built; S20 issuer-level option-positioning aggregates built; S22 issuer holder-concentration/HHI metrics built; FIGI mostly null; auto cross-CIK rollups stay advisory | curated subadvisor/M&A continuity seed, N-PORT cross-link, manager portfolio-turnover, broader issuer coverage | thirteenf.py, filer_alias.py, ownership.py, thirteenf_position_metrics.py, thirteenf_option_metrics.py, thirteenf_concentration_metrics.py | 6 (extend) |
| 4 | Insider ownership (3/4/5, 13D/G) | ~15 | insider, insider_relationship, insider_transaction, insider_holding, filing_form4, tradingplan_10b5_1, blockholder_filing, blockholder_reporting_person, insider_transaction_metrics, form144_intent, form144_to_form4_link | S3 SEC ownership/blockholder XML landing tables; S29 issuer-day/trailing-window insider transaction signal metrics with 10b5-1 sale contamination, cluster buys, bitemporal revisions, as-of API, lake/watermarks/quality; S30 Form 144 intent-vs-executed reconciliation with PIT links to Form 4 sales | broad Form 3/4/5 backfill, populated Form 144 feed, N-PORT, N-PX proxy votes, congressional disclosures, pre-2023 10b5-1 inference, household/entity rollups | insider_ownership.py, insider_metrics.py, form144.py | 3 (extend) |
| 5 | Corporate actions | ~10 | corporate_actions (142 rows), corp_action_type_dim, adjustment_factor_history, corporate_action_dividend_metrics, corporate_action_split_metrics, corporate_action_factor_reconciliation, delist_code_dim, delisting_events, delisting_return_observations | div/split factors, cash-dividend analytics, split-event reconciliation, all-event factor reconciliation, public delisting evidence, injectable DLRET-like surface, and PIT vendor-id resolution for observed terminal-return rows | populated observed terminal returns, CRSP DLSTCD reconciliation, name_history, ticker_history, spinoff_basis_allocation, offering, trading_halt | corporate_actions.py, adjustment_factors.py, corporate_action_metrics.py, delisting.py | 5 |
| 6 | Pricing / market data | 6 | equity_daily_bars (**3.18M rows / 8,206 securities, 2012-2014** — S39 broad load), equity_price_metrics (3.18M derived rows), shares_outstanding_history (342 rows), market_cap, enterprise_value, valuation_input_catalog / v_valuation_input_coverage, adjustment_factor_history, daily_adjustment_factors, corporate_action_factor_reconciliation, delisting_events, delisting_return_observations | broad OHLCV + derived price analytics (returns/vol/momentum/liquidity/drawdown/EW-beta/idio-vol/cross-sectional ranks, ~6.5k names/day) plus PIT SEC XBRL share counts including float/treasury/class facts when disclosed as share counts; market cap and component-lineaged EV; valuation input completeness/stub gates; event-level/daily split-total-return factors; all-event factor reconciliation; public delisting proxy rows; injectable observed-return table | quote_eod, bar_intraday, 3-close semantics, populated observed dlret, exchange-sourced daily shares, full spinoff/merger factor policy, wider date window (2014+) for price×fundamental overlap | ticker_history.py (incl. recycled-ticker/share-class collision repair), equity_price_metrics.py, shares_outstanding.py, valuation_multiples.py, enterprise_value.py, adjustment_factors.py, daily_adjustments.py, corporate_action_metrics.py, delisting.py | 5 |
| 7 | ESG / sustainability | 5 | — | — | **ALL**: esg_metric_dim, esg_metric, esg_score, esg_controversy, ghg_emission | none | 7 |
| 8 | Reference classifications | ~8 | taxonomy (3), taxonomy_node (125), taxonomy_mapping (256), entity_classification | S1 SIC/Fama-French/NAICS taxonomy trees + crosswalks populated offline on the live DB; entity_classification now offline-injectable via CIK→SIC CSV (S8a) | populated entity_classification for the full universe (needs SIC source), isic/nace dims, RBICS-style multi-tag | reference_classifications.py | **2** |
| 9 | Off-exchange / short-interest | ~7 | finra_short_interest (66k rows), short_interest_metrics, finra_short_volume, short_volume_metrics, offexchange_venue, offexchange_volume, offexchange_security_period, offexchange_quality_report | short interest populated; S7a ATS/OTC transparency, S26 daily short-volume, and S28 quality report built as injectable surfaces (default empty until files) | live FINRA OTC/daily short-volume connectors + historical backfills, borrow-fee | finra.py, short_interest_features.py, short_interest_metrics.py, short_volume.py, offexchange.py, offexchange_quality.py | 5 |
| 10 | Supply-chain + Macro | sc_node/sc_edge + macro (~5) | macro_series, macro_observations (40k rows), macro_metrics (53,964 rows) | macro built incl. derived rates/regime metrics; supply-chain absent | sc_node, sc_edge, sc_node_observation, customs/CBP-AMS, Exhibit-21 subsidiary graph, GLEIF/LEI loader | macro.py, macro_metrics.py | 9 (last) |

**One-line read:** Fundamentals, 13F, FINRA short interest, macro, reference classifications, insider/blockholder landing tables plus insider transaction metrics, and the estimates schema are real. Corp-actions/pricing now include adjustment and delisting evidence surfaces. Remaining large gaps are populated licensed/public estimate feeds, ESG, deeper off-exchange data, and the strategic supply-chain wedge.

**PF-S2 operations update:** Rebuild orchestration is now built in-repo. `DatasetOrchestrator` builds a deterministic dataset-id DAG from `DATASET_REGISTRY` metadata, records one parent manifest in reconciled `etl_job_runs`, writes one `etl_job_steps` row per node, appends `etl_job_audit` actions, applies watermark-driven incremental skips, supports exponential retry/backoff, resumes pending/failed steps by `run_id`, and preserves partial/failed manifests with terminal `run_fail` audit rows. `scripts/warehouse_jobs.py run-all` delegates to this path and adds `--full-rebuild` / `--resume <run_id>` while keeping `run` and `run-dataset` on the single-dataset operator path. This is orchestration-only: no dataset compute/output, ratio math, PIT timestamps, `DEFAULT_CONCEPTS`, or non-fundamental-domain data surfaces changed. Verification for this PF-S2 close was offline pytest only; no live SEC/FRED/FINRA/OpenFIGI/GLEIF smoke was run.

**PF-S3 concept-coverage update:** Concept extraction is now governed by the active S3 statement-map projection and committed `db/seeds/concept_map.csv` (us-gaap/dei only; IFRS remains excluded). Overlay gaps are explicit in `fundamental_statement_overlay_allowlist`, and migration `0069` adds catalogued reports for concept coverage, unmapped concepts/items, and the `fundamental_xbrl_metric` vs `fundamental_ratios` security-universe gap. The new quality gates fail on future unmapped non-allowlisted concepts and on any ratio security absent from the xbrl-metric universe. This PF-S3 close is offline code/test only; no operator companyfacts re-fetch, xbrl_metric rebuild, ratio rebuild, or live DB smoke was run in this step.

**PF-S5 identifier-spine update (S5-0..S5-4, closed):** The reference-master identifier spine now carries a sticky corporate `entity_id` above share-class `security_id` (S5-0, migrations `0079`/`0080`), an offline OpenFIGI CUSIP→FIGI alias loader with ambiguous-match routing to the resolution ledger (S5-1, `db/identifiers_figi.py`), an offline GLEIF LEI loader attaching `LEI` aliases at the entity level plus Level-2 parent-edge ingestion with ambiguous cik↔lei routing (S5-2, `db/identifiers_lei.py`, migration `0081`), and `sec_company_facts.entity_id`/`security_id` resolved PIT through the spine rather than passed through raw (S5-3, migration `0082`). **S5-4 (this close)** hardens the spine: (1) a new `export_scan_internal_cusip_leak` quality check (`db/quality.py`) makes the `internal_cusip`-never-exported boundary (13f_holdings.md §B.3; field_catalog note on migration `0079`) an enforced invariant instead of tribal knowledge — it fails if any `lake.DEFAULT_EXPORT_OBJECTS` member ever carries an `internal_cusip` column; (2) `dedupe_open_identifier_intervals` is now wired into all three seeder write paths (`upsert_security_master_from_frame`, `identifiers_figi.py`, `identifiers_lei.py`) so they can never reintroduce an open-ended duplicate, and migration `0083` is a one-time repair (mirroring migration `0054`'s `_repair_identifier_history_overlaps`) that drove the reaccumulated `identifier_same_source_self_overlaps` standing failure back to 0; (3) `_apply_accepted_13f_holdings` (`db/identifier_decisions.py`) now runs the same `_reject_unresolvable_targets` guard S5-1 added for `_apply_accepted_identifiers`, closing the same UNRESOLVED-CUSIP-sentinel corruption exposure on the 13F decision-apply sink. This close is offline code/test only; live OpenFIGI/GLEIF smoke (real bulk files) is operator-pending, and the shared default `db/atx_impl.duckdb` file is not migrated as part of this step (see `WAREHOUSE_PARITY_TRANCHES.md` for the tranche row and verification commands).

---

**PF-S7 validation update (S7-0..S7-3, code landed):** XBRL validation is now dimension-aware and carries per-row dimensional evidence plus a reportable `resolution_status` (migration `0089`) on `xbrl_validation_results`. Calculation-linkbase rows distinguish complete footings (`resolved_ok`), positively explained dimensional artifacts (`resolved_dimensional_artifact`, still `status='passed'` without widening tolerance), and residual genuine errors (`genuine_error`). The warehouse quality check and `XbrlValidationDataset.load` now report the genuine calculation-error residual rather than counting resolved dimensional artifacts as failed checks, and expose split details for total calculation rows, resolved dimensional artifacts, genuine calculation errors, DQC rows, and DQC failures. S7-1 also added declarative referential quality checks, and S7-2 added a small SQL-native DQC subset; DQC violation rows use `resolution_status='genuine_error'`. This update was verified offline only. No live DB smoke was run, so the historical 1,364-row live triage remains operator-pending and no live split counts are claimed here.

**PF2-S1 platform/schema-management update (BUILT/PARTIAL):** schema-as-contract is now present as a committed warehouse surface rather than a missing governance idea. `db/schema_contract.py` derives the declarative manifest from `schema.py` and `migrations.py`, persists it to the new `schema_contract` table, exposes a stable `schema_contract_sha256`, and powers `detect_schema_drift`. Migrations `0097`/`0098` add the table, seed/catalog metadata, and indexes; migration `0099` adds the catalogued `v_warehouse_catalog` view. Critical quality checks now enforce manifest-driven catalog completeness and PIT-column presence, and `warehouse_catalog_asof(...)` plus `python -m db.asof warehouse-catalog --as-of ...` expose the warehouse catalog as of a point in time. The PF2-S1 close was verified offline only (`python -m pytest atx-impl/db/tests -q` passed at final head); no live DB migration/apply/smoke was run, so live drift counts and the shared-DB `schema_contract_sha256` remain operator-pending. Remaining platform gaps are deliberately PF2-S2/PF2-S10 work: migration checksum enforcement, apply-locking, approved backup/checkpoint/restore governance, and gated orchestrator halting on critical checks. Known caveat: the new PIT-column-presence check intentionally surfaces pre-existing live fact tables that lack required PIT columns, especially `is_latest_revision`; the offender set was pinned/ratcheted and should be fixed or explicitly exempted by S10 quality gating/backfill work.

**PF2-S2 platform/migration-governance update (BUILT/PARTIAL):** migration governance now enforces the append-only invariant and provides a scripted backup/restore path. `db/migrations.py` computes source checksums for every registered migration body plus direct helper sources, backfills/writes `schema_migrations.checksum`, verifies checksums on hot `DuckDBStore.initialize()` fast-paths and before applies, and uses the `migration_apply_lock` sentinel row to fail fast on concurrent migration attempts. Migrations `0100`/`0101` add/catalog `migration_apply_lock`, `migration_backup_registry`, and lookup indexes. `db/migration_admin.py` adds checkpoint, timestamped `.duckdb`/`.wal` backup, registry recording, persisted-contract schema verification, restore, WAL-failure recovery, and registered-only backup retention. `scripts/warehouse_migrate.py` is the governed operator path: claim lock, checkpoint, close/reopen for Windows-safe file backup, apply pending migrations, verify the PF2-S1 persisted contract, verify checksums, record backup metadata, and restore on failed verification. Offline/temp-DB verification passed (`test_migration_governance.py`, `test_migrations.py`, and a temporary `warehouse_migrate.py` smoke to schema version `0101`). No live 14 GB shared-DB migration/apply/smoke was run, so the live backup path, exact backup bytes/SHA, and restore proof remain OPERATOR-PENDING. PF2-S10 still owns quality-check halt behavior in the orchestrator and the PIT-column gap backfill/exemption policy.

**PF2-S3 standardized-fundamentals update (BUILT/PARTIAL):** the warehouse now has a deterministic standardization engine, the largest remaining Compustat/FactSet fundamentals-parity gap. `db/seeds/standardization_rules.csv` carries 300 declarative rules (126 annual, 126 TTM, 48 instant) over the canonical item template, including coalesce/direct rules plus sum/difference derivations such as `total_debt` and `net_change_debt`. `db/standardization.py` loads those rules, evaluates them with a closed dispatch table (no `eval`/`exec`), materializes `fundamental_standardized`, records unrouted tags in `fundamental_standardization_exception`, and preserves PIT availability as the max input `available_at`. Migrations `0103`-`0106` add/catalog the standardized fact, indexes, exception table, and `v_fundamental_standardization_coverage`; the persisted `schema_contract` is refreshed so the new PIT surfaces are drift-checked. `fundamental_ratios.load_ratio_inputs` now prefers standardized item-id pivots and falls back to the legacy raw TTM/statement/XBRL pivots, preserving existing ratio math and availability computation while allowing the standardized surface to drive ratios. Critical quality gates now enforce standardization exception rate and template coverage. Offline verification passed (`test_standardization.py`, `test_fundamental_ratios.py`, `test_migrations.py`, `test_migration_governance.py`, schema-contract tests, quality smoke, and full `db/tests -q -n0`). No live standardized rebuild/proof slice was run, so row count, coverage %, exception count, and run_id remain OPERATOR-PENDING.

**PF2-S4 PIT snapshot/vintage update (BUILT/PARTIAL):** month-end reconstruction and ratio-vintage history now exist as governed offline surfaces. `db/pit_snapshot.py` materializes `fundamental_pit_snapshot`, selecting the latest fact vintage visible at an end-of-month boundary (`available_at <= month_end_ts`, no future-period facts) and tagging `as_first_reported`, `most_recently_restated`, or `intermediate_restatement` by accession vintage. Migrations `0107`-`0109` add the snapshot table, `fundamental_ratios.vintage_class`, lookup indexes, catalog rows, and refreshed schema-contract metadata. `fundamental_ratios` gains opt-in `FundamentalRatiosOptions(vintage_mode="history")`, which writes one ratio row per TTM accession vintage while preserving the default latest-revision rebuild path. `fundamental_ratios_asof_month(...)` and `pit_snapshot_asof(...)` expose month-end reads, with CLI branches `query_asof --view fundamental-ratios-asof-month` and `--view pit-snapshot`. Offline verification passed (`test_pit_snapshot.py`, `test_fundamental_ratios.py`, migration/governance tests, schema-contract tests, quality smoke, and full `db/tests -q -n0`). No live shared-DB migration, PIT snapshot rebuild, or one-year proof slice was run, so snapshot row counts, ratio-vintage counts, and example live as-of-month outputs remain OPERATOR-PENDING.

**PF2-S5 industry-template update (BUILT/PARTIAL):** bank/insurance/REIT overlays are now promoted into governed industry-template surfaces, with new utility and broker-dealer profiles. `db/industry_templates.py` seeds the `industry_template` and `industry_template_item` dimensions, materializes PIT-stamped `entity_industry_template` routing from SIC classifications, and refreshes `industry_template_coverage`. Migrations `0110`-`0113` add/catalog those tables and indexes. `fundamental_statement_points` refresh now reads the materialized route table, `fundamental_statement_map` carries utility/broker-dealer rows plus REIT FFO/AFFO derived metadata, and new critical quality checks enforce exactly-one route plus required-item coverage. `formula_registry.csv` now carries bank, broker-dealer, insurance, utility, and REIT family rows without formula-engine dispatch changes. Offline verification passed (`test_industry_templates.py`, concept coverage, item registry, formula library, ratios, schema contract/quality smoke, and full `db/tests -q -n0`). No live shared-DB route rebuild or proof slice was run, so routed-template counts, coverage counts, and derived FFO/AFFO live examples remain OPERATOR-PENDING.

**PF2-S6 calendarization/TTM update (BUILT/PARTIAL):** fiscal-to-calendar normalization now exists as governed offline surfaces. `db/calendarization.py` implements deterministic FYR/period-length helpers, materializes `fundamental_calendar_map` with fiscal-reported, containing-calendar, and greatest-overlap labels plus `week_count`/`is_53_week`, builds `fundamental_calendar_ttm` for PIT-visible quarter-like statement flows on shared calendar quarters, and refreshes `calendarization_coverage`. Migrations `0114`-`0116` add/catalog the map, calendar-TTM, coverage, and indexes with schema-contract refreshes. `refresh_fundamental_ttm_points` now tags annual-minus-9mo Q4 paths as `stitched_quarterly_ttm` while preserving the trailing-400-day guard. Critical quality checks enforce exactly-one calendar labels, 53-week flagging, no duplicate calendar-TTM windows, no duplicate stitched fiscal-TTM windows, and aggregate coverage green status. Offline verification passed (`test_calendarization.py`, period-date/restatement/schema-contract/import related tests, and full `db/tests -q -n0`). No live shared-DB proof slice was run, so calendar map rows, calendar-TTM rows, non-Dec-FYE relabel counts, 53-week flags, stitched-TTM windows, and `run_id` remain OPERATOR-PENDING.

**PF2-S7 segment/footnote update (BUILT/PARTIAL):** the dimensional half of cached inline-XBRL is now mined into governed additive surfaces instead of being silently discarded by the consolidated extractor. `db/segments.py` builds `segment_dim` and `segment_fact` over dimensioned facts for business/geographic/product/customer segment sales, operating income, assets, D&A, and capex, with PIT lineage, revision flags, major-customer typed-member capture, and a default 2% tolerance-banded reconciliation to `fundamental_standardized` / `fundamental_xbrl_metric` (`reconciled`, `flagged_divergent`, `no_consolidated`; divergence is a warning, not a failure). `db/footnotes.py` builds `footnote_pension`, `footnote_deferred_tax`, `footnote_lease`, and `footnote_sbc` for pension/OPEB, deferred-tax, ASC 842/pre-842 lease, and aggregate/award-type SBC details; per-executive grant detail remains out of scope. Migrations `0117`-`0120` add/catalog the segment tables, footnote tables, indexes, and `segment_footnote_coverage`, with schema-contract refreshes. Quality checks cover malformed segment/footnote rows, duplicate segment revision keys, reconciliation divergence warnings, and coverage-count validity. Offline verification passed (`test_segments.py`, `test_footnotes.py`, import/jobs/schema-contract checks, and full `db/tests -q -n0`). No live shared-DB proof slice was run, so segment/footnote row counts, reconciliation pass/flag/no-consolidated split, coverage row, and `run_id` remain OPERATOR-PENDING.

**PF2-S8 preliminary press-release / estimate-basis update (BUILT/PARTIAL):** the announcement-day earnings number now has a governed offline surface instead of being absent until the final 10-Q/K. `db/press_release.py` ingests injectable 8-K Item 2.02 / EX-99 text or normalized rows into `press_release_facts`, extracting preliminary revenue, EPS, operating-income, and net-income figures with source-file SHA, evidence text, confidence, basis, and release-time `available_at`. `press_release_reconciliation` links those preliminary facts to later final `est_actual` rows while retaining both vintages and updating `fundamental_periods.pdate`/`rdq` to the earlier flash date. `est_actual` now carries a `basis` column stamped `GAAP` for SEC companyfacts rows, and `est_surprise` carries `actual_basis`, `consensus_basis`, and `basis_mismatch`, suppressing `surprise_pct` when tagged bases differ. Migrations `0121`-`0123` add/catalog these surfaces and basis extensions with schema-contract refreshes; migration `0123` avoids same-transaction secondary-index recreation around DuckDB's `est_surprise` ALTER dependency guard for legacy-forward fixtures. Quality checks cover malformed press-release rows, no-lookahead release timestamps, retained preliminary vintages, missing EPS actual basis, and mismatch rows with populated `surprise_pct`. Offline verification passed (`test_press_release.py`, `test_estimates.py`, import/jobs/schema-contract checks, and full xdist `db/tests`: 955 passed in 188.32s). No live shared-DB proof slice or press-release corpus load was run, so preliminary-capture counts, basis coverage, and `run_id` remain OPERATOR-PENDING.

**PF2-S9 valuation/reconciliation/DQC update (BUILT/PARTIAL):** valuation overlap is now persisted as `valuation_overlap_slice`, a PIT provenance row carrying numerator/denominator security counts, overlap date span, coverage ratio, stale price/fundamental gap counts, and details JSON for each `valuation_multiples` refresh. New `db/fact_disagreement.py` loads injectable Sharadar/SimFin-style baseline facts into `vendor_baseline_facts` and writes `fact_disagreement` rows comparing latest visible vendor and `fundamental_standardized` vintages on `(security_id,item_id,period_end,basis)` with `agrees` / `disagrees` / `missing_warehouse` statuses and a first-class >99% agreement quality gate. `xbrl_validation.py` still leaves calculation-linkbase `absolute_tolerance=1.0` unchanged and appends additive SQL-native DQC subset rows for DQC_0004, DQC_0080, DQC_0018, and DQC_0041, in addition to the existing DQC_0015/DQC_0053 subset; migration `0126` catalogs the ported/skipped rule boundary. Migrations `0124`-`0127` add/catalog/index the new S9 surfaces and refresh the schema contract. Offline verification passed (focused valuation/reconciliation/DQC/jobs tests, migration/schema-contract tests, and full `db/tests`: 964 passed in 168.68s). No live shared-DB valuation proof slice, vendor baseline load, or DQC-family count run was performed, so agreement ratio, overlap counts, and DQC row counts remain OPERATOR-PENDING.

**PF3-S1 platform/backfill-rails update (BUILT/OFFLINE-PROVEN):** the warehouse now has code-proven backfill and incremental-maintenance rails on offline fixtures. `db/backfill.py` supplies a deterministic windowed partition planner, resumable/idempotent `backfill` and `maintenance` drivers, per-partition watermarks, bounded stale-partition scheduling, retry/dead-letter handling, and partition status accounting. Migrations `0132`-`0134` add/catalog `backfill_run`, `backfill_watermark`, `backfill_dead_letter`, indexes, and `v_backfill_status`; `DatasetOrchestrator` exposes governed backfill/maintenance entry points with bounded fan-out through the existing `etl_job_*` manifest path; and `scripts/warehouse_backfill.py` provides operator `backfill`, `resume`, and `status` commands. This is platform rail work only: no live full historical backfill or live proof slice was run, no live row counts/run IDs are claimed, and the first content consumer remains PF3-S4's price backfill riding these rails.

**PF3-S2 schema-contract v2 update (BUILT/OFFLINE-PROVEN):** schema-as-contract now has the PF3 semantic and export-contract layer. Migration `0135` closes the known PIT-column gap by backfilling meaningful bitemporal columns and registering reasoned `pit_exemption` rows; `pit_column_presence` now targets zero unhandled offenders. Migration `0136` persists `unit`, `sign`, `scale`, and `natural_key` into `schema_contract` and widens the contract hash. Migration `0137` pins `schema_contract_version='v2'` to the live manifest hash, seeds a factor-panel-centric `panel_contract` stub for the future long factor stream (`security_id`, `as_of_date`, `factor_id`, `value`, availability/provenance columns), and registers the critical `semantic_contract` quality check. `semantic_contract_check` fails incomplete semantic declarations, non-finite constrained values, negative non-negative values, and out-of-bounds unit-interval/bounded domains while allowing legitimate signed negatives; registry-disabled checks suppress the scan before execution. Verification was offline only (`test_schema_contract_v2.py`, schema-contract + migration-governance regression, and full `atx-impl\db\tests -q` all passed). No live shared-DB migration/apply/smoke was run, so live PIT-offender counts, semantic-check headline, version pin, and run_id remain OPERATOR-PENDING.

**PF3-S3 architecture decomposition update (BUILT/OFFLINE-PROVEN):** the largest DB modules are now packageized without changing their public API or warehouse behavior. `db.migrations`, `db.quality`, `db.asof`, and `db.estimates` split into focused package files while preserving migration version/checksum identity, quality check-spec identity, as-of reader imports, star-import behavior, and facade monkeypatch compatibility for existing tests. `db/module_boundaries.py` adds a repo-native boundary guard that snapshots the public API for `db` plus the decomposed packages, detects cross-package private imports, checks source import cycles, and pins the expected API in `db/tests/data/public_api_snapshot.json`. Verification was offline only with focused regression suites and the bounded smoke runner; no schema migration, live shared-DB apply, rebuild, row-count change, or operator run was performed.

**PF3-S4 PIT universe + price backfill update (BUILT/OFFLINE-PROVEN):** the warehouse now has a governed interval-keyed common-equity/liquidity universe (`universe_membership`, migration `0140`), a S1-driven historical bar backfill client (`bulk_daily_bars_backfill` in `db/pricing_bulk.py`) with price-specific partition evidence (`price_backfill_partition`, migration `0141`), a no-lookahead `universe_membership_asof` reader plus `v_price_fundamental_overlap` evidence view (`0142`), and a critical `priced_fundamental_universe_decision_coverage` quality gate with S4 lookup indexes (`0143`). The new universe reconciles to the legacy `universe_memberships` liquidity snapshot instead of replacing it, and the backfill path is fixture-proven through the PF3-S1 resumable/idempotent partition driver. Verification was offline only with focused S4/quality/schema-contract/migration checks and `scripts\db_dev_tests.py --smoke`; no live shared-DB migration/apply, archive backfill, row-count change, overlap-density live count, or operator run was performed.

**PF3-S5 valuation input completeness update (BUILT/OFFLINE-PROVEN):** pricing/corporate-action parity now has the missing valuation-input spine. Migration `0144` extends `shares_outstanding_history` for `float`, `treasury`, and common share-class facts when the source fact is share-denominated, plus quality enforcement that float shares cannot exceed same-vintage shares outstanding. Migration `0145` adds `enterprise_value` with explicit component lineage (`market_cap + total_debt + preferred_equity + minority_interest - cash_and_equivalents`), as-of reader support, job wiring, and sign validation. Migration `0146` makes injectable observed delisting-return rows resolve missing `security_id` through PIT `security_identifier_history` without lookahead. Migration `0147` adds the data-owned `valuation_input_catalog`, normalized `v_valuation_input_coverage`, and critical quality gates for universe-day core input completeness plus empty-surface stub detection. Verification was offline only with focused share/EV/delisting/quality/schema-contract/migration checks and bounded smoke; no live shared-DB migration/apply, EV rebuild, observed terminal-return load, or valuation input coverage count is claimed.

**PF3-S10 backtest factor-panel export update (BUILT/OFFLINE-PROVEN):** the S8/S9 factor namespace now has a governed export boundary. Migrations `0164`-`0167` add `v_factor_panel` and `v_factor_panel_wide`, register `v_factor_panel` as a date-partitioned lake export with expected schema hash metadata, register the critical `factor_panel_export_contract` gate, and add panel-read indexes plus `factor_panel_wide` catalog metadata. `db.factor_panel` now provides pure panel assembly, latest-as-of reads, wide pivots, `describe`, a guarded `export_factor_panel`, and `python -m db.factor_panel {describe,read,export}`. `LakehouseExporter` enforces registered schema hashes before write, and `validate_lake_export` validates partitioned export manifests. The S10 export gate fails on undeclared columns, bad panel-contract metadata, row-level future availability, future input-lineage availability, and non-member rows before export. Verification was offline only with focused panel/export tests, storage-admin lake regressions, migration/schema-contract/module-boundary checks, and `git diff --check`; no live shared-DB migration/apply, panel export, schema hash, partition count, or run_id is claimed.

**PF4-S1 signal-evaluation surface update (BUILT — fixture-proven; live-dense pending PF4-S6):** the exported `v_factor_panel` now has a governed scoring layer over it, read-only. Migrations `0176`-`0179` add `factor_eval_manifest` (alpha-backtest-manifest-shaped lineage: `_hash_id`/`params_json`/`source`/`run_id`), `factor_ic`/`factor_ic_decay` (per-factor rank-IC aggregated cross-sectionally per date across the horizon ladder, with an IC-decay profile and never a cross-date pooled correlation), `factor_quantile_spread`/`factor_turnover` (decile long-short spread, hit-rate, decile monotonicity, and rebalance-to-rebalance top/bottom-decile membership churn plus rank autocorrelation), `factor_correlation`/`factor_crowding` (pairwise cross-sectional factor correlation and a derived per-factor crowding score against the rest of the namespace), and `factor_breadth` (per-date non-null coverage over the as-of universe, with all five canonical PIT columns carried physically — no `pit_exemption`). New `db/signal_eval.py` implements every `compute_*` transform as a deterministic, row-order-invariant pure function (property-tested under input shuffles) plus `persist_*` writers and `evaluate_panel(store, run_id=...)` orchestration. Migration `0179` adds the gated factor DQC: `factor_dqc_result` plus two `severity=critical` `quality_check_registry` rows — `factor_leakage_tplus0` (adversarial same-day-return probe; RED on a planted leaky factor, GREEN on a properly lagged one) and `factor_coverage_asof_universe` (RED on a sparse factor, GREEN on well-covered) — wired into `db/quality/_runner.py` via a lazy existence-gated hook (`signal_eval_dqc_results`) that a planted-red result routes `evaluate_quality_gate` to `halt`. All nine tables are schema-as-contract catalogued (`table_catalog` + `field_catalog` rows; IC/correlation/breadth metric columns land with auto-catalog `unit=NULL` — richer unit/sign/scale semantics is a follow-up needing a future migration number, S1's `0176`-`0179` are exhausted). Verification was offline/fixture-proven only (`python -m pytest db\tests\test_signal_eval.py -q` and the full `python -m pytest db\tests -q` both green modulo pre-existing PF4-S3-owned failures); no live shared-DB migration/apply was run. **Live smoke is OPERATOR-PENDING:** migrations `0176`-`0179` are not yet applied to the live `atx_impl.duckdb`; expected ~0 factor-eval rows on the current DB because the price×fundamental overlap is empty (`equity_daily_bars` 2012-2014 vs fundamentals 2017-2026) — meaningful counts arrive with PF4-S4 (survivorship returns) + PF4-S6 (dense backfill). No automated non-empty end-to-end test yet exercises `evaluate_panel`'s full persistence into `factor_quantile_spread`/`factor_turnover`/`factor_correlation`/`factor_crowding`/`factor_breadth` together (persistence paths are verified manually in review plus per-table `persist_*` unit tests); a seeded non-empty `v_factor_panel` integration test is a candidate for PF4-S2, which also owns promoting the leakage/coverage DQC to orchestrator halt gates plus factor freshness/anomaly/lineage observability.

**PF4-S2 factor-panel gating + observability update (BUILT — fixture-proven; live gate/observability OPERATOR-PENDING):** the exported/scored factor panel is now orchestrator-gated and observable. Migration `0180` adds `panel_gate_config` seeded with PF4-S1's landed DQC names (`factor_leakage_tplus0`, `factor_coverage_asof_universe`) plus `factor_panel_export_contract`, all `critical`; `db/observability.py::evaluate_panel_gate` reuses the unchanged `run_warehouse_quality_checks` + `summarize_quality_gate` and folds the recorded leakage/coverage/export outcomes bounded to the run's step-start timestamp (no re-computation, no stale-row consumption), wired into `DatasetOrchestrator._run_step` as an additive panel branch that leaves the non-panel PF2-S10 path byte-identical: a `critical` panel breach halts the run, `error` degrades it to `partial`, `warning` records only. Migrations `0181`/`0182` add three factor-observability surfaces — `factor_freshness_sla` (per-dataset watermark-lag SLA; `factor_panel` seeded at 3 days, critical), `panel_rowcount_anomaly` (median/MAD z-score collapse detection over the panel cross-section size), and `lineage_completeness_checks` (per-emitted-factor lineage-edge completeness) — each reusing the shared `_median_mad`/`_robust_z`/`quality_check` primitives (no fork) and routing breaches through `severity=critical` into the same gate halt path. `panel_rowcount_anomaly` carries `as_of_date` (a strong bitemporal marker), so it registers a `pit_exemption` for `available_at`/`source_loaded_at`/`run_id`/`is_latest_revision` as a monitoring/detection-results artifact (sibling of `data_quality_checks`). Migration `0183` adds `maintenance_schedule` (cadence-as-data: seed rows for `equity_daily_bars`/`fundamentals`/`vintages`/`factor_panel`) plus `scripts/warehouse_schedule_plan.py`, a strictly `read_only=True` dry-run planner (single SELECT, zero writes) that classifies each dataset as backfill vs maintenance from its `backfill_window_json` shape. All new tables are schema-as-contract catalogued and the schema-contract-v2 pin is refreshed. Verification was offline/fixture-proven only (`db/tests/test_panel_gating.py` plus the full `db/tests` suite, both fast-lane and `--run-slow`, green modulo the pre-existing PF4-S3-owned failures); **live is OPERATOR-PENDING** — migrations `0180`-`0183` are not applied to the live `atx_impl.duckdb`, so the gate, observability sweeps, and schedule plan are dormant until the panel is populated by PF4-S4 (survivorship returns) + PF4-S6 (dense backfill). This flips the "factor panel is ungated / unobservable" gap: the panel is now orchestrator-gated (leakage/coverage/export criticals halt runs), observable (freshness/anomaly/lineage), and its maintenance cadence is a queryable fact; the residual is live archive execution (operator) plus the S1 leakage/coverage check-name reconciliation already folded into `panel_gate_config`, and the `module_boundaries` public-API snapshot re-pin (owned by PF4-S3).

**PF4-S3 code-hardening update (S1-S10 review closed; offline suite re-greened):** the S1-S10 code review of the PF4-S1/S2 factor-scoring/gating build is now fully closed against fixture-proven tests, and the eight pre-existing offline-suite failures inherited from PF4-S1/S2 are fixed. The one **High** finding — `enterprise_value` silently reading a stale or missing period across a filing-lag boundary instead of falling back to the latest genuinely *available* period — is closed (`db/enterprise_value.py`). The five **Medium** findings are closed: a hashseed-dependent `manifest_id` is now sorted-and-stable (`db/factors/engine.py`); the factor pivot could double-count a same-day revision instead of selecting exactly one latest-visible row per PIT key (`db/factors/engine.py`); a windowed universe build could miss in-window observations at the window's own start date, now fixed with a `2x lookback_days` buffer (`db/universe.py`); the cross-domain cross-section could double-count a stale revision — and a same-commit fix widened the dedup key so distinct insider-metric `window_days` rows stop colliding (`db/factors/cross_domain.py`); and the EV/growth/cross-domain-lineage hot paths are now vectorized byte-equivalent to their row-by-row reference implementations (`db/enterprise_value.py`, `db/metric_engine.py`, `db/factors/cross_domain.py`). The five **Low** findings are hardened: deterministic panel dedup via stable sort plus `run_id` tiebreak, read-only panel DB connections (`db/factor_panel.py`), a `zscore` non-finite-to-`NA` guard with `neutralize` added to the PIT-safety operator map (`db/factors/cross_section.py`), EV bad-component skip-and-flag instead of refresh abort (`db/enterprise_value.py`), and a regression-lock test pinning `full_rebuild`'s partition deletes to stay window-scoped (`db/tests/test_backfill.py`). Separately, all eight pre-existing failures are closed: three `test_formula_registry_catalog.py` failures were seed drift (not a clock time-bomb) from PF3-S6's committed 66-row `formula_registry` catalog, plus two real time-bombed tests de-time-bombed to a frozen `AS_OF`; the S4a authorized-item-id coverage test is re-pinned to 143 ids; the factor-catalog-count test now self-derives its expected count instead of hardcoding it; two concept-coverage tests are fixed by root-causing and closing a genuine PF3-S5 cross-sprint seed defect (six share-count statement-map rows renumbered `1044`-`1049` to `1045`-`1050`, six matching registry items seeded, `1044` stays a reserved unauthorized gap id); and the `module_boundaries` public-API snapshot is re-pinned (see `WAREHOUSE_PARITY_TRANCHES.md` for the audited delta). The full offline suite is green and time-bomb-free on both the fast lane and the `--run-slow` gate lane, with zero date/clock-conditioned skips (all remaining skips are `@pytest.mark.slow`). **Parity content is unchanged by this entry** — no domain moved fill level, no table in the Executive Summary changed, and no migration was added (migration `0184` remains reserved and unused); this is a code-hardening/determinism/re-green close, not new build.

## 2. Per-Domain Detail

### Domain 1 — Fundamentals (XBRL / us-gaap)  ·  Status: BUILT (polish remaining)

**Target tables** (`datasets/fundamentals_us_equities.md`, `data_models_and_methodology.md` Part A/G.2):
- `fund_fact` / long-form EAV — `(entity, security, period_id, item_id, value)` bitemporal core.
- `fund_item` — canonical item dictionary (us-gaap tag → ats-eqt item_id, parent, calc_formula).
- `fund_period` — period dim with the four-date PIT model: `datadate`, `rdq` (8-K Item 2.02 release), `fdate` (10-K/Q filed), `ldate` (last vintage).
- `xbrl_concept_map` — us-gaap/IFRS concept → canonical metric, with extension classification.
- `entity.industry_template` — gating field (INDL vs FS) routing bank/insurance/utility/broker-dealer/REIT templates and Nareit/FFO overlays.

**Current state — mostly BUILT:**
- `sec_company_facts` (companyfacts.json facts) — `fundamentals.py`, **~2.47M us-gaap facts over ~1,489 CIKs after S43** (was ~12k over 5; us-gaap/dei only — IFRS `ifrs-full` issuers excluded).
- `fundamental_points` / `fundamental_statement_points` (**~2.5M / ~1,598 securities**) / `fundamental_ttm_points` (**1,496,175 / 1,395 securities**) / `fundamental_periods` / `fundamental_fact_revisions` — bitemporal restatement chain with `as_of_date`/`available_at`, now over ~1,400-1,600 securities (S43). This already implements the "as-first-reported → restated" two-tier + revision audit (`is_latest_revision`, `previous_value`, `value_delta`). The derived `fundamental_ratios` surface spans **933,564 rows / 1,395 securities / 33 companyfacts-derived ratio codes** — a Compustat-scale cross-section; the 21 `xbrl_filing_facts`-derived codes (liquidity/leverage/health scores) remain 5-security until that cache widens.
- `fundamental_statement_map` S4a cross-industry seed (143 authorized item_ids as of PF4-S3 — the prior 137 plus the six PF3-S5 share-count item_ids `1045`-`1050` (public float, treasury shares, Class A-D common shares outstanding), renumbered off the reserved unauthorized gap id `1044`; multiple rows where concepts coalesce) plus 47 governed industry overlay/template item_ids for bank, insurance, REIT, utility, and broker-dealer = the `xbrl_concept_map` equivalent — backed by `fundamental_statements.py` seed.
- Full XBRL substrate: `xbrl_concept_catalog`, `xbrl_taxonomy_packages/roles/relationships`, `xbrl_dimension_edges`, `xbrl_fact_frames` (1,204), `xbrl_filing_contexts` (6,878), `xbrl_filing_dimensions` (14k), `xbrl_filing_facts` (25k) — `xbrl_taxonomy.py`, `xbrl_filing_contexts.py`.
- **PF-S1 canonical item spine:** `fundamental_item`, `fundamental_item_alias`, and `fundamental_item_vendor_map` now provide the governed item dictionary and vendor cross-walk. S1-4 migration `0064` additively links both `fundamental_statement_points.item_id` and raw `fundamental_points.item_id` to that spine where taxonomy/concept mappings are covered; unmapped concepts are surfaced as warnings, while any concept mapping to more than one item_id is a failing quality check. The link is reference metadata only and leaves PIT timestamps/value columns unchanged.
- **PF-S3 concept-coverage controls:** `DEFAULT_CONCEPTS` derives from the active, loadable statement-map projection; `concept_map.csv` is the reviewable seed; S3-1 bank/insurance/REIT overlay exceptions are enumerated with reasons; S3-2 widened the companyfacts path for `fundamental_xbrl_metric`; S3-3 migration `0069` adds the catalogued `fundamental_concept_coverage_report`, `fundamental_unmapped_concept_report`, `fundamental_statement_overlay_allowlist`, and `fundamental_xbrl_metric_ratio_universe_gap` views plus quality gates. Verification was offline fixtures only; live coverage percentages require an operator re-fetch/rebuild.
- **PF2-S3 standardized fundamentals:** `fundamental_standardized` is now the comparable long-format item fact over the governed item template, built by declarative `standardization_rules.csv` and pure `compute_standardized_rows`. It accounts for source tags via standardized rows or `fundamental_standardization_exception`, exposes `v_fundamental_standardization_coverage`, and feeds `fundamental_ratios` through standardized item-id pivots before falling back to raw source tables. This closes the code/schema side of the standardization gap; live proof-slice counts and exception/coverage rates are still operator-pending.
- **PF2-S5 industry templates:** `industry_template`, `industry_template_item`, `entity_industry_template`, and `industry_template_coverage` now make template routing and item-set coverage queryable. Utility (`UT`) and broker-dealer (`BD`) overlays are seeded with loadable us-gaap concepts where available and explicit vendor-only allowlist entries where not; REIT FFO/AFFO can be derived when reported Nareit FFO is absent.
- **PF2-S6 calendarization:** `fundamental_calendar_map`, `fundamental_calendar_ttm`, and `calendarization_coverage` now make fiscal-to-calendar mapping, 52/53-week flags, calendar-aligned TTM, and stitched quarterly-TTM coverage queryable. The fiscal TTM refresh tags annual-minus-9mo Q4 stitch rows as `stitched_quarterly_ttm` on rich proof-slice inputs and no-ops on thin caches.
- **PF2-S7 segments/footnotes:** `segment_dim`, `segment_fact`, `footnote_pension`, `footnote_deferred_tax`, `footnote_lease`, `footnote_sbc`, and `segment_footnote_coverage` now mine dimensioned inline-XBRL facts into additive segment and footnote surfaces with PIT lineage and warning-only segment reconciliation flags.
- **PF2-S8 press-release/basis:** `press_release_facts` and `press_release_reconciliation` retain preliminary 8-K Item 2.02 figures, populate flash `pdate`/`rdq`, and extend `est_actual`/`est_surprise` with GAAP-vs-street basis tags and mismatch-safe surprise percentages.
- **PF2-S9 valuation/reconciliation/DQC:** `valuation_overlap_slice`, `vendor_baseline_facts`, and `fact_disagreement` now track overlap coverage and injectable cross-vendor agreement; XBRL validation has a wider SQL-native DQC subset plus a catalogued ported-vs-skipped rule ledger.

**Gap:**
- `fundamental_statement_map`'s blueprint-sourced S4a scope from `cross_vendor_field_map.md` sections 2.1-2.4 is **137** cross-industry item_ids; the pinned `AUTHORIZED_S4A_ITEM_IDS` set is now **143** — those 137 plus the six PF3-S5 share-count item_ids `1045`-`1050`, with `1044` reserved — and S4b/S5 adds 47 bank/insurance/REIT/utility/broker-dealer overlay item_ids.
- Industry-template routing and coverage are built, but some template items remain intentionally non-loadable vendor/extension/Nareit entries in the explicit allowlist. Calendarization and stitched-TTM code paths are built and fixture-proven, but live proof-slice counts remain operator-pending.
- Four-date PIT is implicit (`as_of_date` + `available_at`) but `rdq` (press-release date from 8-K Item 2.02) is **not separately captured** — needed for contemporaneous earnings-reaction studies.
- **Remaining quality gaps:** full Arelle/XBRL US DQC 150+ execution, loaded vendor-baseline breadth for `fact_disagreement`, and live proof-slice counts. Dimension-aware calculation-linkbase validation, referential checks, an expanded SQL-native DQC subset, and the injectable cross-vendor reconciliation table are built; live residual triage counts are still operator-pending.
- **Connectors present** (SEC companyfacts.json, Frames, taxonomy packages). **Missing:** DERA Financial Statement Data Sets bulk (SUB/NUM/TAG/PRE) for pre-API backfill; Sharadar SF1 / SimFin as reconciliation baselines.

**S4/S5 implementation update:** S4a/S4b/S4c/S4d cover 143 cross-industry item_ids as of PF4-S3 (the prior 137 plus the six PF3-S5 share-count item_ids `1045`-`1050`, with `1044` remaining a reserved unauthorized gap id), 37 bank/insurance/REIT overlays, `fundamental_periods.datadate/rdq/pdate/fdate/ldate`, and SQL calculation-linkbase validation rows in `xbrl_validation_results`. S5 promotes overlays into routed template tables, adds utility/broker-dealer item sets, derives REIT FFO/AFFO, and adds industry-template coverage gates. Remaining fundamentals gaps are full Arelle/DQC 150+ rule execution, loaded cross-vendor baseline breadth for `fact_disagreement`, richer EX-99 press-release actual extraction, and live proof-slice validation.

**Known live triage item -- calculation-linkbase:** before PF-S7, the `failed_xbrl_calculation_linkbase_checks` check reported **1,364** failed rows over only **5 securities / 11 filings**, characterized as structural dimensional-context mismatches rather than a tolerance bug. PF-S7 S7-0..S7-3 has now landed the code path needed to triage that set: parent and child facts are compared only within comparable period/unit/dimensional signatures, each row carries `dimensional_evidence_json`, and `resolution_status` separates `resolved_dimensional_artifact` from `genuine_error`. The reporting headline now counts only the genuine-error residual. This session did **not** run a live DB refresh/smoke, so the actual live split of the historical 1,364 rows remains operator-pending; no live residual count is fabricated here. The tolerance remains `1.0`.

**S9a implementation update — calculated items (Compustat/FactSet ratio parity):** `fundamental_ratios` (migration `0032`, `db/fundamental_ratios.py`) is the derived "calculated items" surface — a long-format PIT fact (one row per `security_id,ratio_code,basis,period_end`) computed from the bitemporal TTM flows (`fundamental_ttm_points`) over the instant balances (`fundamental_statement_points`). v1 ships 15 ratios across profitability (net/operating margin, ROA, ROE), leverage (assets/equity, liabilities/assets, liabilities/equity), cash-flow (free_cash_flow level, fcf_margin, ocf/net_income, capex/revenue), payout (dividend payout, buyback/NI, total payout), and per-share (book_value_per_share). Each ratio carries its **own** `available_at` = max availability of its specific inputs (true per-fact PIT), a `numerator_value`/`denominator_value` lineage pair so `value` is reproducible, and an `is_meaningful` flag (false when a denominator is non-positive, e.g. ROE on negative equity). As-of reader `fundamental_ratios_asof` + `query_asof --view fundamental-ratios`; depends on `sec_company_facts` in the DAG. **S9b** expands coverage to **23 ratios** (no schema change, same engine), adding an `efficiency`/activity family (`asset_turnover`, `equity_turnover`) plus cash-flow-coverage and reinvestment ratios (`operating_return_on_assets`, `operating_cash_flow_margin`, `operating_cash_flow_to_assets`, `operating_cash_flow_to_liabilities`, `capex_to_operating_cash_flow`, `retention_ratio`) — all computable from the existing 15 canonical metrics, so they materialize real rows immediately. **S9c** adds a `growth` family (5 YoY ratios — `revenue_growth_yoy`, `net_income_growth_yoy`, `operating_income_growth_yoy`, `operating_cash_flow_growth_yoy`, `assets_growth_yoy`) computed by a pure-pandas prior-year pairing (`_attach_prior_year`, 350–380-day window) over the same TTM/instant inputs; `ratio_kind='growth'` (value = (current − prior)/|prior|, flagged not-meaningful off a non-positive base). **S9d** reuses the prior-year pairing to add average-balance returns (`average_return_on_assets`, `average_return_on_equity`, `operating_cash_flow_to_average_assets` — denominator = mean of ending and prior-year balance, the Compustat return-on-capital convention) plus `equity_growth_yoy`. **S10a** opens concept coverage *offline*: `fundamental_xbrl_metric` (migration `0033`, `db/fundamental_xbrl_metrics.py`) extracts the **consolidated** (non-segment — fact context has zero dimension members) us-gaap balance concepts the narrow companyfacts feed omits, straight from the already-cached `xbrl_filing_facts` (no network). It maps `AssetsCurrent`/`LiabilitiesCurrent`/`CashAndCashEquivalentsAtCarryingValue`/`InventoryNet` → `current_assets`/`current_liabilities`/`cash_and_equivalents`/`inventory`, bitemporal with restatement vintages (`is_latest_revision`, `available_at` = filing acceptance). The ratio loader gains a `balx` pivot over this table, unlocking a **`liquidity`** family: `current_ratio`, `quick_ratio` (inventory coalesced to 0 for non-inventory firms), `cash_ratio`, `working_capital` (new `difference` kind), `working_capital_to_assets`. Live: **7,793 rows / 37 ratio codes**; `fundamental_xbrl_metric=66`; AAPL @2026-03-28 current_ratio=1.0704, quick_ratio=1.0202, cash_ratio=0.3385, working_capital=$9.473B. As-of reader `fundamental_xbrl_metric_asof` + `query_asof --view fundamental-xbrl-metric`; DAG `fundamental_xbrl_metric` (← `xbrl_filing_contexts`) feeds `fundamental_ratios`. **S10b** extends the extractor to `LongTermDebt → long_term_debt` and adds a debt/solvency slice to the `leverage` family: `long_term_debt_to_equity`, `long_term_debt_to_assets`, `net_debt` (long-term debt minus cash, `difference` kind), `net_debt_to_assets`. Live: `fundamental_xbrl_metric=88`, `fundamental_ratios=7,851` over **41 ratio codes** (BA long_term_debt_to_equity=7.844 — thin equity; ADBE net_debt=−$117M — net cash positive). **S10c** extends the extractor to **annual (≈365-day) duration flow concepts** (`GrossProfit`→gross_profit, `CostOfGoodsAndServicesSold`/`CostOfRevenue`→cost_of_revenue, `InterestExpense`→interest_expense, `DepreciationDepletionAndAmortization`→depreciation_amortization), keyed on (period_start, period_end) — the annual-window filter sidesteps the Q-vs-YTD-vs-FY ambiguity, so these compute at fiscal-year period_ends where the annual flow aligns with TTM revenue/operating_income. New `profitability`/`leverage` ratios: `gross_margin`, `cost_of_revenue_to_revenue`, `interest_coverage`, `ebitda` (operating_income + D&A, level), `ebitda_margin`. Live: `fundamental_xbrl_metric=117` (88 instant + 29 duration), `fundamental_ratios=7,888` over **45 ratio codes** (AAPL gross_margin=0.4691, ebitda=$144.7B, ebitda_margin=0.3478; BA gross_margin=0.0479). **S10d** adds a **financial-health** family: a `RetainedEarnings` instant extract feeds `retained_earnings_to_assets` and `equity_to_liabilities`, and a new `score` ratio_kind carries the **Altman Z″** composite distress score (1995 non-manufacturer variant, book equity so no price needed: 6.56·WC/TA + 3.26·RE/TA + 6.72·EBIT/TA + 1.05·equity/TL). Live: `fundamental_xbrl_metric=139`, `fundamental_ratios=8,153` over **48 ratio codes / 9 families**; Altman Z″ ranks BA=1.29 (grey/stress), AAPL=3.37 (safe), ADBE=7.35 (safe) — economically correct. **S10e** adds the **Piotroski F-score** (`piotroski_f_score`, a second `score`-kind composite in the `health` family): nine year-over-year binary signals — ROA>0, CFO>0, ΔROA>0, accruals (CFO>NI), Δlong-term-leverage<0, Δcurrent-ratio>0, no net share issuance, Δgross-margin>0, Δasset-turnover>0 — summed 0–9. The eight delta signals reuse the `_attach_prior_year` pairing (now extended to long-term debt, current assets/liabilities, gross profit, and a new period-end shares metric); the share-issuance signal is sourced from a new `CommonStockSharesOutstanding → common_shares_outstanding` XBRL **instant** extract (the dei cover-page share count in `fundamental_statement_points` is dated at the filing, not the period close, so it does not pair year-over-year). Because the score needs the current *and* ~1y-prior value of every input (including the annual `gross_profit` flow), it emits at fiscal-year period_ends with a complete prior year. Live: `fundamental_xbrl_metric=161` (added 22 `common_shares_outstanding`), `fundamental_ratios=8,155` over **49 ratio codes**; **AAPL FY2025 F=8** (high quality), **BA FY2025 F=7** (recovering) — economically sensible. **S10f** adds the **Ohlson O-score** (`ohlson_o_score`, a third `score`-kind composite in the `health` family): the 1980 nine-term bankruptcy-probability logit (−1.32 − 0.407·ln(assets) + 6.03·TLTA − 1.43·WCTA + 0.0757·CLCA − 1.72·OENEG − 2.37·NITA − 1.83·FUTL + 0.285·INTWO − 0.521·CHIN). It needs only current balances/flows + prior-year net income (already paired by `_attach_prior_year`) — **no new XBRL concept** — so it emits wherever current assets/liabilities and a prior year exist (broader than Piotroski: no annual-gross-profit dependency). The SIZE term's GNP-deflator is omitted (assets in USD — a constant additive shift that preserves the cross-sectional ranking). Live: `fundamental_ratios=8,165` over **50 ratio codes** / **10** Ohlson rows across AAPL/ADBE/BA; modeled P(default) AAPL/ADBE≈0.000, BA≈0.001 (highest — leveraged, thin equity), consistent with the Altman ranking. **S10g** extends the instant extractor to `PropertyPlantAndEquipmentNet → property_plant_equipment_net` and `AccountsReceivableNetCurrent → accounts_receivable`, adding three standard `efficiency`/activity ratios: `fixed_asset_turnover` (revenue/net PP&E), `receivables_turnover` (revenue/receivables), `ppe_to_assets` (capital intensity). Live: `fundamental_xbrl_metric=199`, `fundamental_ratios=8,208` over **53 ratio codes**; the cross-section captures business models cleanly — `ppe_to_assets` AMZN=0.436 (capital-heavy) vs ADBE=0.062 (asset-light software) vs BAC=0.004 (bank), `fixed_asset_turnover` ADBE=13.5 vs AMZN=2.0. **Gap / next:** quarterly-TTM stitching of the flow concepts was **feasibility-checked and deprioritized** — the cached consolidated duration windows are 3-mo/6-mo/12-mo only (no ~9-mo YTD bucket) and the 3-mo facts are sparse (≤3 securities), so neither the annual−9mo reconstruction nor a trailing-four-quarter sum yields enough complete TTM windows to be worthwhile until the security universe is widened (network-gated, build/smoke only); `interest_coverage` is sparse until consolidated annual `InterestExpense` is reported; price-based valuation multiples (P/E, P/B, EV/EBITDA) remain blocked on the live DB because the cached `equity_daily_bars` sample (2012–2013) does not temporally overlap the fundamentals (2017–2026); ratio v1 still stores only the latest-revision input vintage (restatement-vintage ratio history is the planned non-breaking refinement).

**S27 implementation update — Beneish M-score:** `fundamental_xbrl_metric` now maps combined annual SG&A concepts (`SellingGeneralAndAdministrativeExpense` / `SellingGeneralAndAdministrativeExpenses`) to `selling_general_and_administrative_expense`, and `fundamental_ratios` adds `beneish_m_score` as the fourth `health`/`score` composite. The score uses DSRI, GMI, AQI, SGI, DEPI, SGAI, TATA, and LVGI with the existing per-input availability watermark and prior-year pairing. Live no-migration rebuild: `fundamental_xbrl_metric=202`, `fundamental_ratios=8,209` over **54 ratio codes**; one AAPL FY2025 Beneish row emitted (`-2.293696`, available at `2026-05-01 22:00:00`). Coverage is conservative: G&A-only facts are not treated as SG&A, so the score widens only when combined SG&A or a future component-summing metric is available.

**PF2-S6 note superseding the S10g quarterly-TTM gap language above:** the code/schema wall is now resolved for proof-slice inputs. `refresh_fundamental_ttm_points` tags Q4 = annual minus 9-month YTD windows as `stitched_quarterly_ttm`, and `fundamental_calendar_ttm` materializes common-calendar trailing windows with duplicate-window gates. The live data wall is still operator-pending: the default/shared DB was not migrated or rebuilt here, so actual 9-month YTD coverage, stitched-window counts, and calendar-TTM rows must be recorded by a backed-up proof-slice run before claiming live breadth.

**PF2-S7 note on segment/footnote breadth:** the code/schema wall is resolved for dimensional inline-XBRL mining, but live breadth remains bounded by the cached primary-document filings. Segment totals are intentionally reconciled with tolerance and surfaced as warnings because ASC 280 segment definitions drift and often do not foot exactly to consolidated totals. The default/shared DB was not migrated or rebuilt here, so live segment/footnote coverage and reconciliation split must be recorded by a backed-up proof-slice run before claiming production breadth.

**PF2-S8 note on preliminary press-release breadth:** the code/schema wall is resolved for fixture-proven preliminary capture and basis-aware surprise, but live breadth requires an operator-supplied 8-K/EX-99 corpus. The default/shared DB was not migrated or rebuilt here, so live preliminary capture rate, `pdate`/`rdq` flash-date updates, and EPS basis coverage must be recorded by a backed-up proof-slice run before claiming production breadth.

**PF2-S9 note on valuation/reconciliation/DQC breadth:** the code/schema wall is resolved for overlap provenance, offline vendor-baseline reconciliation, and wider SQL-native DQC rows, but live breadth still requires a backed-up proof-slice run with overlapping price/fundamental data and an operator-supplied Sharadar/SimFin-style baseline. The default/shared DB was not migrated or rebuilt here, so live `valuation_overlap_slice`, `fact_disagreement` agreement ratio, and DQC row-count splits must be recorded before claiming production breadth.

**PF2-S10 note on production gating/observability:** the platform capstone is now implemented in code/schema. `quality_check_registry` makes thresholds and severity data-driven, `data_quality_checks.severity` is persisted, and `DatasetOrchestrator.run(..., gate=True)` consumes checks: `critical` halts, `error` degrades the final run to `partial`, and `warning` records only. Freshness SLAs, row-count anomaly events, storage stats, backup-before-compact, partitioned/incremental lake exports, and `warehouse_rebuild_runs` are fixture-proven offline. The Compustat standardized-fundamentals parity row in `db/parity.py` is flipped to `implemented` for code/schema parity. Live proof remains operator-pending: intentionally trip/revert one critical gate, record real DB size/storage stats, run a freshness sweep, exercise a partitioned lake sample, and run a deterministic rebuild from the primary tree after PF2-S2 backup governance.

### Domain 2 — Estimates (analyst / IBES / consensus)  ·  Status: PARTIAL

**Target tables** (`datasets/estimates.md` §9, `data_models_and_methodology.md` Part B):
- `est_broker` + `est_broker_alias` — estimator registry with vendor-ID vintage tracking (IBES reshuffle).
- `est_analyst` + `est_analyst_alias` — analyst registry, masked/named, vintage dates.
- `est_measure_dim` — EPS/SALES/EBITDA/CFPS/DPS/BPS/TGT/REC canonical codes.
- `est_period_dim` — FY/FQ/CY/LT/NT with FPI encoding (`fpi_ibes` 0..9/A/B/Y), `fpedats` anchor, expected/confirmed release dates.
- `est_detail` (a.k.a. `est_fact`) — long-format triple `(broker_id, analyst_id, measure_id, fpi, fpedats, value)` + `anndats`/`actdats`/`revdats` + `basis` (P/D) + `is_gaap` + `estimate_type` + bitemporal.
- `est_consensus` — mean/median/high/low/stdev/N + revision counts, snapshotted at `statpers`/`consensus_date`.
- `est_actual` — as-reported actuals per period.
- `est_recommendation` — 1=StrongBuy..5=Sell (canonical IBES scale) + price target.
- `est_guidance` — 8-K Item 2.02/7.01 NER-extracted company guidance (no us-gaap XBRL element exists).

**Current state — PARTIAL BUILT:**
- `est_measure`, `est_actual`, and `est_surprise` are real: SEC companyfacts actuals map into canonical estimate measures and SUE is computed PIT-safely from originally reported actuals.
- `est_detail` now has an injectable CSV loader for IBES-like or normalized detail rows, with `anndats`/`actdats`/`revdats`/`stop_date`, source-file hashes, broker/analyst alias dimensions, `est_period_dim`, watermarks, lake exports, quality checks, and `est_detail_asof`.
- `est_consensus` now has an injectable CSV loader for IBES `statsumu`-style or normalized summary rows, with `statpers`/`fpedats` aliases, stable snapshot ids, source-file hashes, `est_period_dim` updates, 105-day stale windows, watermarks, quality checks, and latest-snapshot `est_consensus_asof`.
- `est_recommendation` now has an injectable CSV loader for IBES `recddet`/`ptgdet`-style or normalized recommendation and price-target rows, with canonical IBES rating scale, broker/analyst aliases, source-file hashes, active-window as-of semantics, watermarks, and quality checks.
- `est_recommendation_summary` now has an injectable CSV loader for IBES `recdsum`/`ptgsum`-style or normalized recommendation-distribution and price-target summary snapshots, with canonical IBES-direction rating means, provider scale-direction metadata, source-file hashes, watermarks, quality checks, and `est_recommendation_summary_asof`.
- `est_security_link` now reconciles estimate vendor security identifiers to warehouse `security_id` using PIT-visible `security_identifier_history` and accepted `identifier_resolution_decisions`, records accepted/conflict candidate evidence, promotes accepted vendor-id links back into identifier history when enabled, and lets `est_detail_asof`, `est_consensus_asof`, `est_recommendation_asof`, and `est_recommendation_summary_asof` resolve `security_id` only after the link's own `available_at`.
- `est_guidance` now supports both injectable normalized rows and local SEC 8-K Item 2.02/7.01 text corpora. The deterministic extractor captures conservative revenue/EPS/operating-income/net-income ranges or point values, records `guidance_type`, unit/currency scale, `source_item`, extraction confidence, evidence text, source-file hashes, and PIT `available_at` inherited from acceptance time or local row metadata.

**Gap:**
- Populated licensed/public estimate feeds, accepted estimate crosswalk rows, and SEC guidance text corpora are not in the default DB. Full parity still needs real IBES/FactSet/CIQ/Zacks detail/summary/recommendation/price-target files, a scaled 8-K exhibit backfill, and parser extensions for open-ended guidance, non-calendar fiscal years, and more measures.
- **Connectors:** primary public path is SEC EDGAR 8-K Item 2.02 (results) + 7.01 (Reg-FD guidance) prose extraction; secondary public aggregators (Zacks/Yahoo/Nasdaq — license-murky, sourced from IBES). Vendor truth (IBES via WRDS `ibes.detu_epsus`/`statsumu_epsus`/`recddet`, FactSet Estimates API, Bloomberg BEst, CIQ `ciqEstimateNumericData`) is licensed.
- **Quality gotchas to encode:** IBES broker-ID reshuffle (2018: 13.8% brokers / 30.7% analysts reassigned — never join on numeric broker_id across vintages); `revdats`-bumped FPI gotcha (filter `anndats ≤ D ≤ revdats`, join on `fpedats` not `fpi`); **Bloomberg recommendation scale inverts IBES** (5=Buy vs 1=Buy); P-vs-D EPS mixing (~5–8% silent error); stopped-estimate filter (`estimate_type != 'S'`); 105-day stale scrub on consensus.

### Domain 3 — Ownership-13F  ·  Status: BUILT (entity-resolution extension remaining)

**Target tables** (`datasets/13f_holdings.md`): `filer_13f`, `filer_13f_cik_alias`, `filing_13f`, `holding_13f`.

**Current state — BUILT:**
- `thirteenf_managers` (10,672) ← `filer_13f`; `thirteenf_manager_reports` (11,761) ← `filing_13f`; `thirteenf_security_positions` (10,564) ← `holding_13f`; `thirteenf_security_ownership` (54) = security-side QoQ rollup (a bonus aggregate). Loaders: `thirteenf.py`, `ownership.py`.
- **`thirteenf_position_metrics` BUILT (S16)** — manager-side conviction-flow surface (migration `0039`, `db/thirteenf_position_metrics.py`): per-(manager_id, security_id, report_period) QoQ common-share change, `position_action` ∈ NEW/ADDED/TRIMMED/UNCHANGED/EXITED (exits inferred from `thirteenf_manager_reports` filed-quarter set), and `voting_sole_pct` concentration. Complements the issuer-side `thirteenf_security_ownership`. Full house wiring (`thirteenf_position_metrics_asof` + `query_asof --view thirteenf-position-metrics`, jobs DAG ← `sec_13f_ownership_features`, 2 quality checks, lake, watermarks, build script). Live: **6,595 rows / 6,032 managers / 54 quarters** (392 real continuations + 8 exits; NEW-heavy because the cache's pre-2026 consecutive coverage is sparse — deepens as more historical quarters load).
- **`thirteenf_option_metrics` BUILT (S20)** - issuer/report-period 13F option-positioning surface (migration `0043`, `db/thirteenf_option_metrics.py`): aggregate CALL/PUT manager counts, position counts, share-equivalent and reported-value exposure, put/call ratios, common-share denominators, top call/put managers, option-bias labels, and QoQ net-call flow. Full house wiring (`thirteenf_option_metrics_asof` + `query_asof --view thirteenf-option-metrics`, jobs DAG from `sec_13f_ownership_features`, quality checks, lake, watermarks, build script). Live: **22** AAPL issuer-period rows from 2020-12-31 through 2026-03-31 (18 `CALL_HEAVY`, 4 `PUT_HEAVY`), with max `available_at=2026-05-29 22:00:00`.
- **`thirteenf_concentration_metrics` BUILT (S22)** - issuer/report-period 13F common-holder concentration surface (migration `0045`, `db/thirteenf_concentration_metrics.py`): top holder/top 3/5/10 value share, value/share HHI, effective holder count, concentration bucket, and QoQ HHI/holder-count changes. Full house wiring (`thirteenf_concentration_metrics_asof` + `query_asof --view thirteenf-concentration-metrics`, jobs DAG from `sec_13f_ownership_features`, quality checks, lake, watermarks, build script). Live: **54** AAPL issuer-period rows from 2012-12-31 through 2026-03-31; latest quarter has 6,013 holders, value HHI 0.0611, top-holder share 19.24%, top-5 share 43.93%, and max `available_at=2026-05-29 22:00:00`.
- Schema already handles the hard parts: `put_call`, `investment_discretion`, `voting_auth_sole/shared/none`, `share_quantity_type`, `is_amendment`/`amendment_type`, `is_confidential_omitted`, `portfolio_weight`, `available_at` bitemporal.

**Gap:**
- **`filer_13f_cik_alias` BUILT (S6a)** — bitemporal CIK alias / entity-resolution spine in `filer_alias.py`: SELF identity per filer CIK, intra-CIK `NAME_HISTORY`, low-confidence cross-CIK `NAME_MATCH_CANDIDATE` links, and an injectable curated `SUBADVISOR`/`MA_CONTINUITY`/`MANUAL` seed. `resolve_primary_cik` defaults to authoritative-only (confidence ≥ 1.0) so same-name candidates never silently merge. Live build: 10,672 SELF + 99 candidate rows (e.g. Dimensional Fund Advisors across 3 CIKs, D.E. Shaw, Citigroup). Remaining: load a curated continuity seed for true subadvisor/M&A rollups; auto-derived cross-CIK links stay advisory.
- **2023-01-03 value-unit cutover RESOLVED (S6b)** — `thirteenf.value_unit_multiplier` / `apply_value_unit_cutover` scale `value_usd` to whole dollars at holdings load (×1000 for `period_of_report ≤ 2022-12-31`, via an accession→period map). No live change (all loaded periods are post-2023, factor = 1); the fix protects future pre-2023 backfills from a 1000× SUM understatement. Regression-tested in `test_thirteenf_value_cutover.py`.
- **`figi` resolution infrastructure BUILT (PF-S5 S5-1/S5-4)** — `db/identifiers_figi.py` is an offline injectable OpenFIGI CUSIP→FIGI/TICKER alias loader with ambiguous-match conflict routing to `identifier_resolution_candidates`/`identifier_resolution_decisions`, and the decision-apply sink (`_apply_accepted_13f_holdings`) is now guarded against writing an unresolved/bogus `target_security_id` into `thirteenf_holdings.security_id` (S5-4). The default DB still has 0 real OpenFIGI rows and `thirteenf_holdings.figi` is still largely null — a real OpenFIGI bulk-mapping file has not been injected (operator-pending, network-gated, out of the pytest path).
- **Missing:** N-PORT cross-link (`fund.adviser_cik → filer_13f.primary_cik`) — depends on Domain 4; Form SHO 13f-2 short table (defer to ~2027, first filings 2028-02-14).

### Domain 4 — Insider ownership (Form 3/4/5, 13D/G, N-PORT, 144)  ·  Status: PARTIAL

**Target tables** (`datasets/insider_ownership.md` Part I):
- `insider`, `insider_relationship` — person dim + (insider×issuer×role) bitemporal.
- `insider_transaction` — Form 4/5 fact with the **28-letter `transaction_code`** (P=highest-signal buy; S split by `rule_10b5_1_indicator`; F/M cashless-exercise netting; J/K footnote-NER), `is_derivative`, `acquired_disposed`, `shares_owned_following`, `direct_indirect`.
- `filing_form4` — metadata + `footnotes` JSON (load-bearing for 10b5-1, family-trust, vesting).
- `blockholder_filing` + `blockholder_reporting_person` — Schedule 13D/G, group-filing flag, `type_of_reporting_person` (BD/BK/IC/IA/...), voting/dispositive power, `purpose_text` (Item 4 activist intent).
- `fund`, `fund_class`, `filing_nport`, `fund_holding` — N-PORT fund-level holdings (units NS/PA/NC/OU, asset_cat, payoff_profile, securities-lending).
- `form144_intent` + `form144_to_form4_link` — intent-vs-executed reconciliation.
- `tradingplan_10b5_1` — plan registry + `cooling_off_compliant`.
- `proxy_vote` (N-PX), `congressional_disclosure` (STOCK Act PTR).

**Current state - PARTIAL BUILT:**
- `filing_form4`, `insider`, `insider_relationship`, `insider_transaction`, `insider_holding`, and `tradingplan_10b5_1` are built in `db/insider_ownership.py` from SEC Forms 3/4/5 ownership XML, with bitemporal `available_at`, issuer/owner IDs, derivative/non-derivative rows, transaction code, acquired/disposed flag, direct/indirect ownership, holdings, footnotes, and post-2023 10b5-1 plan indicators/adoption dates.
- `blockholder_filing` and `blockholder_reporting_person` are built for Schedule 13D/G XML, including reporting-person type, beneficial ownership, voting/dispositive power, source-of-funds/purpose text, and PIT filing availability.
- **S29 insider transaction metrics BUILT:** `insider_transaction_metrics` (migration `0051`, `db/insider_metrics.py`) materializes one row per `(source, security_id, signal_date, window_days)` with open-market purchase/sale counts, plan-vs-discretionary sale splits, 10b5-1-heavy-sale flag, trailing cluster buyer/seller counts/value, discretionary sell-pressure flag, source transaction lineage JSON, and preserved late-filing revisions by `available_at`. Full wiring: `insider_transaction_metrics_asof`, `query_asof --view insider-transaction-metrics`, jobs DAG after `sec_insider_ownership`, watermarks, lake export, and 3 quality checks. Current shared DB has 13 derived rows from the loaded Section 16 sample.
- **S30 Form 144 reconciliation BUILT:** `form144_intent` and `form144_to_form4_link` (migration `0052`, `db/form144.py`) provide an offline/injectable Form 144 XML/CSV intent surface with source-file hashes, seller CIK/name normalization, proposed shares/value/price, sale-window, 10b5-1 plan fields, latest/restatement markers, and `as_of_date`/`available_at` PIT semantics. The reconciliation links visible Form 144 notices to subsequent Form 4 open-market sale rows by insider/security/date window, carrying match method, confidence, share/value completion, and PIT-safe availability. Full wiring: `form144_intents_asof`, `form144_reconciliation_asof`, `query_asof --view form144-intents|form144-reconciliation`, `scripts/build_form144.py`, jobs DAG after `sec_insider_ownership`, watermarks, lake export, and Form 144 quality checks. Shared DB is migrated through `0052`; live row counts are currently 0 until Form 144 source files are injected.

**Gap:**
- **Connectors:** SEC EDGAR Ownership XML (Form 3/4/5 mandatory XML since 2003, ~250k/yr; spec v5.1); Schedule 13D/G **structured XML mandate 2024-12-18** (pre-date needs HTML/ASCII parser — two pipelines); N-PORT CSV bulk (`sec.gov/data-research/.../form-n-port-data-sets`); Form 144 XML (mandatory 2023-04-13, loader is now offline/injectable but not broadly populated); N-PX XML (2024-08-31); STOCK Act via House Clerk / Senate Ethics + Senate/House Stock Watcher JSON. Reuse: `edgartools`.
- **Quality gotchas:** transaction_code enumeration is the load-bearing field; 13D deadline compressed to 5 business days (2024-02-05); 10b5-1 cover-page indicator from 2023-04-01 + 90/120-day cooling-off; N-PORT monthly-public timing is a moving target (delayed to 2027-11-17 / 2028-05-18, Feb-2026 proposal may scale back); CUSIP→FIGI on 13D/G.

### Domain 5 — Corporate actions  ·  Status: PARTIAL

**Target tables** (`datasets/corporate_actions.md`): `corp_action`, `corp_action_type_dim`, `adjustment_factor`, `delisting`, `delist_code_dim`, `name_history`, `ticker_history`, `spinoff_basis_allocation`, `offering`, `trading_halt`.

**Current state — PARTIAL:**
- `corporate_actions` (142 rows) — single flat table: `action_type`, `ex_date`, `declaration_date`, `record_date`, `payable_date`, `cash_amount`, `split_from`/`split_to`, `adjustment_factor`. Loader `corporate_actions.py` (also writes `tbltickerhistory_daily`).
- **S18 derived cash-dividend analytics (`corporate_action_dividend_metrics`, migration `0041`, `db/corporate_action_metrics.py`):** the first *derived* layer on corporate actions and a genuine **cross-domain join** (corporate actions × pricing) — one row per (security_id, ex_date) cash dividend, joined to the ex-date close in `equity_daily_bars` (the two feeds temporally overlap, 2012-04..2013-03). Derives `dividend_yield_spot` (cash / price), `ttm_dividend` / `ttm_dividend_count` (365-day rolling), `dividend_yield_ttm` (the standard income-factor input), and `dividend_growth_yoy`. **Caught + handled a data-quality trap:** the upstream `cash_dividend_inferred` rows are inferred from the ex-date price drop and cannot tell a split from a dividend, so a 2:1 split reads as a ~50%-of-price "dividend" (KO Aug-2012 = $39.41, NKE Dec-2012) — the loader drops any event whose spot yield exceeds 25% of price (a principled split-artifact filter) so it never pollutes the TTM sum, while keeping legitimate large special dividends. Full house wiring: `corporate_action_dividend_metrics_asof` + `query_asof --view corporate-action-dividend-metrics`, jobs DAG (← `corporate_actions`, `daily_bars`), 2 quality checks, lake export, watermarks, build script. Live: **140 rows over 36 securities** (2 split artifacts excluded); TTM yields are all economically correct — KO 3.36%, T 5.15%, VZ 4.71%, INTC 4.18%, and COST 7.96% (its real Dec-2012 $7 special dividend, correctly retained). `dividend_growth_yoy` is NULL across the sample (the 11-month dividend window holds no ~1y-prior event — a coverage limitation, not a defect; it populates once >1 year of dividends loads). Pure offline transform — no network.
- **S19 split-event adjustment reconciliation (`corporate_action_split_metrics`, migration `0042`, `db/corporate_action_metrics.py`):** a second derived corporate-action layer and an explicit adjustment-factor QA surface — one row per normalized split event and bar source. It joins `adjustment_factor_history` split events to the adjacent pre/post rows in `daily_adjustment_factors`, derives the observed daily split price/share factor step, compares it to the event-level `factor_price`/`factor_shares`, and exposes raw vs split-adjusted event-window returns. Rows carry `reconciliation_status` (`RECONCILED`, `MISMATCH`, `MISSING_DAILY_FACTOR`) so missing daily coverage remains visible instead of silently disappearing; `MISMATCH` is a quality failure. Full house wiring: `corporate_action_split_metrics_asof` + `query_asof --view corporate-action-split-metrics`, jobs DAG (← `adjustment_factor_history`, `daily_adjustment_factors`), 3 quality checks, lake export, watermarks, and the generalized `scripts/build_corporate_action_metrics.py --surface splits|dividends|all`. Live: **2 rows** (KO 2012-08-13, NKE 2012-12-26), both `RECONCILED`, zero price/share factor error; raw ~-50% split moves become small split-adjusted returns (-0.28% KO, -2.92% NKE). Pure offline transform — no network.
- **S21 event-level factor reconciliation (`corporate_action_factor_reconciliation`, migration `0044`, `db/corporate_action_metrics.py`):** broadens the S19 split-only audit to every `adjustment_factor_history` event and bar source. It compares split price/share steps, cash-dividend dividend-return steps, and total-return factor steps against adjacent `daily_adjustment_factors`, preserving explicit `RECONCILED`, `MISMATCH`, `MISSING_DAILY_FACTOR`, `COMPOUND_EVENT`, and `UNSUPPORTED_EVENT_TYPE` statuses; `MISMATCH` is a quality failure. Full house wiring: `corporate_action_factor_reconciliation_asof` + `query_asof --view corporate-action-factor-reconciliation`, jobs DAG, 3 quality checks, lake export, watermarks, and `scripts/build_corporate_action_metrics.py --surface factors|all`. Live: **142 rows over 36 securities** (140 `CASH_DIV`, 2 `SPLIT`), all `RECONCILED`; ex-date range 2012-04-03..2013-03-13; split/share/total-return errors are zero and max dividend-return error is ~1.1e-16; watermarks `max_ex_date=2013-03-13`, `max_available_at=2013-03-19 22:00:00`. Pure offline transform — no network.
- `security_identifier_history` + `exchange_listings` partially serve `name_history`/`ticker_history` (bitemporal `valid_from`/`valid_to`).
- `delist_code_dim` + `delisting_events` materialize PIT public delisting evidence from `listing_status_intervals`, with nullable `delisting_return`, explicit `is_return_imputed`, `return_policy`, and source-status lineage.
- `delisting_return_observations` is an injectable observed-return surface for CRSP-like `DLRET`/`DLRETX`/`DLAMT`/`DLPRC` rows; rows that omit warehouse `security_id` can resolve vendor identifiers through PIT `security_identifier_history` without lookahead, and `delisting_events_asof` enriches events only with observations visible at the query timestamp.

**Gap:**
- `delist_code_dim` is a public proxy dimension, not a licensed CRSP DLSTCD map; `delisting_events.delisting_return` is null by default unless an explicit imputation policy is requested.
- No populated observed **`dlret`** terminal-return evidence in the default DB; Shumway-Warther-style -30% unresolved-delete imputation exists only as an opt-in research policy, not as a claimed source value.
- No **`spinoff_basis_allocation`** (Form 8937 / IRC §358), no `offering` (IPO/secondary), no `trading_halt`.
- **Connectors:** present — inferred from local archive. Missing: CRSP `dsedist`/`dsedelist` enumerations (academic gold), DTCC CA 20022, EDGAR Form 8937 (only public spinoff-basis path), Form 25/15 for delistings.

### Domain 6 — Pricing / market data  ·  Status: PARTIAL

**Target tables** (`datasets/pricing_market_data.md`): `bar_daily`, `bar_intraday`, `quote_eod`, `shares_outstanding_history`, `short_interest`, `adjustment_factor_history`.

**Current state — PARTIAL:**
- `equity_daily_bars` (9,834) ← `bar_daily`: `open/high/low/close/adjusted_close/volume/vwap`, `dividend_amount`, `split_factor`, `is_adjusted`, `available_at`. Loader `ticker_history.py`. View `v_equity_daily_returns`.
- **S14 derived daily price analytics (`equity_price_metrics`, migration `0037`, `db/equity_price_metrics.py`):** a typed PIT price-feature surface — one row per (security_id, trade_date) with split/dividend-adjusted daily/log return, overnight gap, trailing realized volatility (20d/60d annualized), momentum (21d/126d), distance from the trailing 252-day high, and dollar volume. **Caught and fixed a data-quality trap in live smoke:** the cached sample's `adjusted_close` column is unreliable (it is an *unadjusted lagged close*, leaving split jumps in — NKE's 2:1 split produced a spurious −51% daily return / 180%-annualized vol). The surface instead computes a proper **back-adjusted close from the bar's own `split_factor`** (reverse-cumulative product of future factors), making the series continuous across splits — NKE's split-day return drops to −2.9% (real) and max realized vol falls to a plausible 62% (AMZN around earnings). Full house wiring: `equity_price_metrics_asof` + `query_asof --view equity-price-metrics`, jobs DAG (← `daily_bars`), 2 quality checks (incl. `pct_from_high_252d <= 0`), lake export, watermarks, build script. Live: **9,834 rows across 40 securities** (2012–13 sample). Standalone price analytics need no fundamentals overlap, so the 2012–13 sample is fully usable here (the overlap gap only blocks price×fundamental multiples). Pure offline transform — no network.
- **S35-S38 price-factor extensions:** `equity_price_metrics` now carries liquidity/capacity (`avg_dollar_volume_21d`, `amihud_illiquidity_21d`), downside risk (`max_drawdown_126d`, `downside_deviation_60d`), market-relative one-factor risk (`market_return_ew`, `beta_60d`, `market_correlation_60d`, `idiosyncratic_vol_60d`), and same-day cross-sectional percentile ranks (`daily_return_cs_pct_rank`, `momentum_21d_cs_pct_rank`, `realized_vol_20d_cs_pct_rank`, `dollar_volume_cs_pct_rank`, `amihud_illiquidity_21d_cs_pct_rank`; migrations `0056`-`0059`). The S37/S38 proxy/ranks are computed over the loaded bar universe; row `available_at` is conservatively delayed to the latest same-day bar used by the proxy. Live after S38: 9,834 rows, 9,794 with daily-return ranks, 9,034 with realized-vol ranks, 8,994 with momentum/Amihud ranks, and 7,434 with beta/correlation/idiosyncratic volatility populated; warehouse quality remains at the two known standing failures.
- `shares_outstanding_history` (342) ← public SEC XBRL `fundamental_statement_points`: `shares_outstanding`, `shares_basic_avg`, `shares_diluted_avg`, plus PF3-S5 `float`, `treasury`, and common share-class rows when the source fact is share-denominated, with `effective_date`, `as_of_date`, `available_at`, accession, revision metadata, as-of reader, watermarks, and quality checks. Loader `shares_outstanding.py`.
- `market_cap` and `enterprise_value` now provide PIT valuation numerators with explicit component lineage. `enterprise_value` is materialized only when market cap, total debt, preferred equity, minority interest, and cash/equivalents are all visible; `valuation_input_catalog` and `v_valuation_input_coverage` turn those inputs plus share-count inputs into a data-owned coverage surface guarded by critical completeness/stub quality checks.
- `adjustment_factor_history` + `daily_adjustment_factors` derive event-level and daily PIT split/total-return factors from normalized `corporate_actions`, with classifier reasons, as-of readers, watermarks, quality checks, lake export coverage, and S21 event-level reconciliation in `corporate_action_factor_reconciliation`.
- `delisting_events` carries public delisting evidence and optional imputation-policy metadata; `delisting_return_observations` can inject observed terminal-return rows, but the default DB has none loaded.
- `short_interest` need served by `finra_short_interest` (Domain 9).

**Gap:**
- **Single source `adjusted_close` remains on `equity_daily_bars`** — split-only and split+dividend total-return close now live in `daily_adjustment_factors`, and `daily_panel_asof` exposes those explicit adjusted-close and adjusted-return semantics. The base `v_equity_daily_returns` view remains raw-close-only for backward compatibility.
- No **`quote_eod`** (bid/ask/spread, close_type A/T/B), no three-close model (official auction vs consolidated tape vs composite).
- **Share-count limits:** `shares_outstanding_history` now covers public XBRL current/basic/diluted plus float/treasury/class facts when those facts are disclosed as share counts, but it is not yet a CRSP daily `SHROUT` equivalent and does not cover exchange-sourced daily shares or split-factor-integrated daily market-cap restatements.
- No **`bar_intraday`**; corporate-action factors are now event-level audited for the cached split/cash-dividend history, but they are still local/public-evidence derived and do not yet include full spinoff, merger, or populated observed delisting-return policies.
- Benchmark-grade beta/market-model parity still needs PIT benchmark/index/ETF total-return feeds and, eventually, sector/industry factor proxies. S37/S38 market-relative features and cross-sectional ranks are internal calculations over the cached 40-security bar sample, not official CRSP value-weighted or benchmark-relative factors.
- **Connectors:** present — local archive only. Missing: CRSP DSF/MSF (delisting-correct), Polygon/Tiingo/Alpaca (modern forward bars), Compustat `co_secd`. PIT integrity: free APIs restate adj_close without snapshot retention — must compute own from raw events.

### Domain 7 — ESG / sustainability  ·  Status: MISSING

**Target tables** (`datasets/esg_sustainability.md`): `esg_metric_dim`, `esg_metric`, `esg_score`, `esg_controversy`, `ghg_emission`.

**Current state:** None.

**Gap (entire domain):**
- `esg_metric_dim` (~1,500 metrics; ESRS/SASB/GRI/TCFD/PAI/GHG-Protocol crosswalk with `pillar` E/S/G).
- `esg_metric` (long-format entity×metric×period, polymorphic value, `source_type` CSRD_ESRS/SEC_10K/CDP/CA_SB253/VENDOR, assurance flags, bitemporal).
- `esg_score` (vendor scores MSCI_IVA/SUSTAINALYTICS_RISK/etc., **`redistributable=FALSE`** — never computed by ats-eqt).
- `ghg_emission` (Scope 1/2/3, per-gas, scope-2 location/market, scope-3 category 1–15, `gwp_basis` AR5/AR6, assurance).
- `esg_controversy` (event-shaped, severity 1–5, monetary impact).
- **Connectors:** SEC 10-K Item 1 (human capital) / Item 1A (climate risk) / Exhibit 21 / Form SD (conflict minerals) / DEF 14A; EU CSRD/ESRS iXBRL (1,144 datapoints, `filings.xbrl.org`); SFDR 18 PAI; CDP; GHG Protocol; California SB 253 (first Scope 1+2 deadline 2026-08-10 = operative US anchor); UK Gender Pay Gap CSV.
- **Gotcha:** vendor ESG ratings are functionally uncorrelated (0.38–0.71) — wedge is **raw disclosed metrics + bitemporal evidence trail**, not another score. SEC Climate Rule is federally dead (pivot to CA SB 253 + 10-K NLP).

### Domain 8 — Reference classifications (GICS/ICB/NAICS/SIC)  ·  Status: BUILT (taxonomy populated; entity coverage pending SIC source)

**S1/S8a update:** `taxonomy`/`taxonomy_node`/`taxonomy_mapping` are built and now populated offline on the live DB (3 taxonomies — SIC, FAMA_FRENCH_12, NAICS_2022; 125 nodes; 256 SIC↔NAICS / SIC-range↔FF12 crosswalk rows). `entity_classification` is built with PIT primary-SIC + derived FF12/NAICS rows and is now populatable **without network** via two injectable offline sources — a CIK→SIC CSV (`sic_file` / `--sic-file`, S8a) and the SEC bulk `submissions.zip` with lazy per-CIK lookup (`submissions_zip` / `--submissions-zip`, S8b) — in addition to the live SEC submissions fetcher. The SEC bulk archive is a one-time operator download (never hit in tests); fixtures cover both offline paths. Default-DB entity_classification remains empty until one of those SIC sources is supplied for the universe. Remaining gaps below.


**S31 update (adjacent reference/listing surface):** `security_listing_metrics` (migration `0053`, `db/listing_metrics.py`) adds a first derived analytics layer on the reference/listing axis — one PIT row per `(security_id, snapshot as_of_date)` joining the CIK-resolved `listing_status_intervals` spine to the `nasdaq_symbol_directory`. It decodes the Nasdaq `financial_status` code (N/D/E/Q + combined G/H/J/K) into `is_deficient`/`is_delinquent`/`is_bankrupt`/`is_noncompliant`/`is_listing_compliant` flags (a delisting-risk signal), plus market-tier, exchange-name, ETF, test-issue, and round-lot classification. Full house wiring (as-of API/CLI, jobs DAG ← `listing_status_intervals`, 3 quality checks, watermarks, lake, FactSet/CRSP security_master parity rows). Live: **12,026** rows over **6,013** securities / 2 daily snapshots, **634** non-compliant names. Caveats: `financial_status` is nasdaqlisted-only; coverage capped at listing-spine-resolved securities; availability = directory snapshot ingest instant.

**Target tables** (`datasets/reference_classifications.md`): `taxonomy`, `taxonomy_node`, `entity_classification`, `taxonomy_mapping`, `sic_code_dim`, `naics_code_dim`, `isic_code_dim`, `nace_code_dim`.

**Current state:** None. SIC codes ride along in EDGAR/XBRL filings but are not extracted into a dim table; `sec_company_tickers` is CIK↔ticker only.

**Gap (entire domain):**
- `taxonomy` (registry with `licensed_flag`/`redistribution_ok`), `taxonomy_node` (hierarchical, bitemporal), `entity_classification` (many-to-many, **`weight` column for RBICS-style multi-tag**, `primary_flag`, source/confidence), `taxonomy_mapping` (many-to-many crosswalks, `mapping_strength`).
- `sic_code_dim` keyed **`(sic_code, authority)`** — SEC fork (8888 Foreign Govts, 9995 Nonclassifiable, 6770 Blank-Check SPAC) vs BLS-1987; CRSP↔Compustat disagree ~36%.
- `naics_code_dim` (public domain, `(naics_code, vintage)`), `isic_code_dim`, `nace_code_dim`.
- **Connectors (all free/redistributable):** NAICS (Census), SIC (BLS-1987 + SEC fork, from EDGAR assigned-sic), ISIC (UN), NACE (Eurostat), Fama-French 12/17/30/48/49 (Tuck/Ken French, SIC-derived), **TRBC via PermID API (the one open index-grade hook)**, Wikidata P452. Closed (alias-only, ACL-gated): GICS, ICB, BICS, RBICS.
- **Gotcha:** all crosswalks many-to-many (GICS-163 → NAICS-6: ~6.5 fan-out); taxonomy revision risk (GICS 2016 RE carve-out, 2018 Comm Services) demands bitemporal storage.

### Domain 9 — Off-exchange / short-interest  ·  Status: PARTIAL

**Target tables** (`datasets/off_exchange_transparency.md` + pricing `short_interest`): `offexchange_volume`, `offexchange_venue`, `offexchange_security_period`, `offexchange_quality_report`, `offexchange_security_period_v`; plus `short_interest`.

**Current state — PARTIAL (short-interest + injectable ATS/OTC transparency + quality controls):**
- `finra_short_interest` (66,266 rows) + `finra_short_interest_backfill_manifests` (2). Loaders `finra.py`, `short_interest_features.py`. Strong: bi-monthly settle-date short interest with feature pipeline.
- **S11 derived short-interest analytics (`short_interest_metrics`, migration `0034`, `db/short_interest_metrics.py`):** a typed, point-in-time analytics surface — one row per (security_id, settlement_date) — turning the raw FINRA feed into the signals quant long/short books trade. Per-security: `short_interest_change`, `short_interest_change_pct`, a recomputed `days_to_cover` (reconciled against the FINRA-reported `days_to_cover_source`), and `short_pct_shares_outstanding` (PIT join to `shares_outstanding_history`). The value-add the raw feed lacks: the **cross-sectional percentile** of days-to-cover and short-interest change within each settlement cohort (`days_to_cover_percentile`, `short_interest_change_pct_percentile`, `universe_count`) — the short-interest anomaly signal. PIT-safe (percentiles ranked within a single settlement cohort, all published together; `available_at` = FINRA publication time). Full house wiring: `short_interest_metrics_asof` + `query_asof --view short-interest-metrics`, jobs DAG node (← `finra_short_interest`, `shares_outstanding_history`), quality checks, lake export, watermarks, `scripts/build_short_interest_metrics.py`. **Live: 62,054 rows across 21,576 securities / 204 settlements** — by far the broadest real surface in the warehouse. Recent settlements rank a ~18k-symbol universe; most-shorted-by-days-to-cover are thin OTC names (huge short vs tiny ADV); AAPL days-to-cover percentile ≈0.6 (moderately shorted). `short_pct_shares_outstanding` is populated only where a PIT share count exists (5 securities → 216 rows) and NULL otherwise. Pure offline transform — no network. **S12 (migration `0035`)** adds the tradeable time-series + tail form: `days_to_cover_zscore` (within-cohort standardized days-to-cover, population std — complements the percentile by capturing tail magnitude; NaN for single-name cohorts), and `days_to_cover_prev` / `days_to_cover_change` (prior-settlement days-to-cover and its change for the same security — rising = building short pressure, lagged per security over the bi-monthly cycle, PIT-safe). Live: 53,390 z-scores + 33,742 trend rows; z-score flags extreme illiquid-name outliers while the percentile stays robust. **S15 (migration `0038`)** adds multi-settlement momentum + a squeeze flag: `short_interest_momentum_3` / `days_to_cover_change_3` (short-interest growth and days-to-cover change over the trailing 3 settlements per security) and `is_squeeze_candidate` (top-decile crowding `days_to_cover_percentile >= 0.90` AND a rising most-recent days-to-cover). **S23 (migration `0046`)** adds `short_interest_momentum_6`, `days_to_cover_change_6`, `short_interest_change_pct_accel`, `days_to_cover_change_accel`, a bounded 0-100 `short_pressure_score`, and `is_persistent_short_pressure` (squeeze candidate plus positive 3-settlement days-to-cover and short-interest momentum). **S24 (migration `0047`)** adds liquidity-aware diagnostics: `average_daily_volume_percentile`, 99th-percentile `days_to_cover_winsorized` and z-score, `liquid_short_pressure_score` (NULL until ADV >= 50k and short position >= 100k), and `is_liquid_short_pressure` (tradeable squeeze candidate with score >= 70). Live after S24: 53,597 winsorized rows, 53,396 winsorized-z rows, 17,964 liquid-score rows, 121 historical liquid-pressure rows; latest settlement 2026-06-15 has 779 squeeze candidates, 11 liquid-pressure names, and max liquid score 85.4161. The raw squeeze/pressure flags intentionally remain unfiltered; S24 adds the tradeability-gated companion signal for downstream books.
- **S7a FINRA OTC Transparency BUILT (injectable):** `offexchange_venue` (MPID dim, ATS vs non-ATS), `offexchange_volume` (per security×venue×period fact with bitemporal availability + FINRA restatement handling via `is_latest`/`restatement_seq`), and `offexchange_security_period` (derived ATS-share rollup with `ats_share_pct`, `ats_venue_count`, `restatement_detected`) plus `v_offexchange_security_period`. Loader `db/offexchange.py` ingests a FINRA `weeklySummary`-shaped or normalized CSV; materializer computes the rollup. End-to-end smoke reproduces the blueprint AAPL fixture exactly (total 66,925,813; ATS 21,246,912; non-ATS 45,678,901; ats_share_pct 31.7470%; 2 ATS venues). Default DB tables are empty until a FINRA file is injected.
- **S26 FINRA daily short-volume flow BUILT (injectable):** `finra_short_volume` stores raw FINRA daily short-sale volume rows by symbol, trade date, and reporting market code with source-file hash lineage, bitemporal `available_at`, restatement sequencing, and latest-vintage flags. `short_volume_metrics` rolls that raw flow to one row per symbol/trade_date with `short_volume_ratio`, `short_exempt_ratio`, dominant reporting market code/share, same-day cross-sectional percentiles, and `is_high_short_flow`. As-of APIs/CLI, jobs, lake exports, watermarks, and 5 quality checks are wired. Default DB is migrated through `0049` and these tables are empty until approved FINRA daily files are injected; isolated smoke loaded 3 FINRA-style fixture rows -> 2 metric rows and validated lake export. Daily short volume is a short-sale **flow** proxy, not a substitute for settle-date short interest.
- **S28 off-exchange quality report BUILT (injectable/control):** `offexchange_quality_report` summarizes the public off-exchange inputs by surface/source/period: row/security/venue-market counts, total/ATS/non-ATS/short volumes, ATS share, short-volume ratio, high short-flow counts, restated-key counts, multiple-latest counts, bad-row and missing-availability counts, max publication lag, source-input JSON, `available_at`, and report revisions. As-of API/CLI, jobs, lake exports, watermarks, and 3 quality checks are wired. Default DB is migrated through `0050`; the report is empty until OTC transparency or daily short-volume rows are injected. Isolated smoke materialized 2 report rows and validated watermarks/quality/lake export.

**Gap:**
- **Live FINRA connectors not yet wired** — the FINRA OTC Transparency API (`POST /data/group/otcMarket/name/{weeklySummary|monthlySummary|blockSummary|weeklySummaryHistoric}`, OAuth2 client-credentials, 10 GB/mo cap, Tier-1 ~14d / Tier-2/OTCE ~28d delay) is still injectable-CSV only; FINRA daily short-volume file ingestion is also injectable/offline, not a live/backfill downloader. Borrow-fee/utilization is commercial (S3/Markit) — skip unless licensed.
- **Gotcha:** restatement semantics (NEW vs AMEND vs METADATA_AMEND) — handled via `is_latest`/`restatement_seq`/`finra_last_update_date`; manager-level shorts (Form SHO 13f-2) not until 2028-02-14.

### Domain 10 — Supply-chain + Macro  ·  Status: MACRO BUILT / SUPPLY-CHAIN MISSING

**Target tables** (`data_models_and_methodology.md` Part C/G.4 + `sources/public_data_sources.md`): `sc_node`, `sc_edge`, `sc_node_observation` (entity-res audit); macro via `macro_series`/`macro_observations`.

**Current state:**
- Macro **BUILT**: `macro_series` (6), `macro_observations` (40,589) with `as_of_date`/`available_at` vintaging. Loader `macro.py` (FRED/ALFRED).
- **S13 derived macro analytics (`macro_metrics`, migration `0036`, `db/macro_metrics.py`):** a typed PIT analytics layer over the raw FRED feed — one row per (series_id, observation_date) with `change_abs` (vs prior obs), `change_yoy` / `yoy_growth` (CPI's `yoy_growth` *is* the inflation rate), and an expanding `zscore` (regime position vs the series' own history), plus a synthetic **`T10Y2Y`** term-spread series (DGS10 − DGS2, the canonical yield-curve recession signal). PIT-safe (YoY looks ~1y back via a ±20-day `merge_asof`; the z-score is expanding — only current+earlier obs); documented caveat that the raw feed is latest-revision FRED, not ALFRED vintages, so macro-revision PIT is approximate (`available_at` = warehouse load). Full house wiring: `macro_metrics_asof` + `query_asof --view macro-metrics`, jobs DAG (← `fred_macro`), 2 quality checks, lake export, watermarks, `scripts/build_macro_metrics.py`. **Live: 53,102 rows across 7 series** (6 raw + T10Y2Y); CPI inflation `yoy_growth` 4.17% (rising, z=2.32 elevated), term spread +0.31 (z=−0.59 flat vs history), unemployment 4.3% (z=−0.8 below average) — all economically correct. Pure offline transform — no network. **S17 (migration `0040`)** adds `expanding_pct_rank` — where each observation sits within the series' own history so far (0, 1], the canonical regime signal ("VIX in its 95th percentile"); complements the z-score with a distribution-free rank robust to fat tails. Live: all 53,102 rows populated; VIX @2026-06-25 sits at the 57th percentile of its history (z=−0.07) — middling regime.
- **S25 real-rate macro extension (`REAL_FEDFUNDS`, migration `0048`):** `macro_metrics` now also emits a synthetic monthly real fed funds series defined as FEDFUNDS minus CPI YoY inflation in percentage points. The synthetic row's `available_at` is the later of the visible FEDFUNDS/CPI inputs, and a new quality check catches missing `REAL_FEDFUNDS` rows when those inputs are available. Live: **53,964 macro_metrics rows across 8 series**, including 862 `REAL_FEDFUNDS` rows; latest 2026-05-01 value is -0.5366146840 percentage points with `available_at=2026-05-01 23:00:00`. Lake, watermarks, as-of API, and quality checks are wired; macro-revision PIT remains approximate until ALFRED vintages replace/latest-augment latest-revision FRED.
- Supply-chain **MISSING**: no `sc_node`/`sc_edge`.

**Gap:**
- `sc_node` (company/subsidiary/plant/port/vessel/mine/material/product/person + geo) and `sc_edge` (supplies_to/customer_of/ships_to/owns, bitemporal, `disclosure_type` D/R/I, `tier`, `confidence`, `share_of_revenue`, `share_of_cogs`, `weight_usd`). `sc_path` as a view, not stored.
- **Connectors (the strategic wedge — vendors overlap only ~43%):** EDGAR Exhibit 21 (subsidiary graph, NLP), 10-K Item 1 customer-concentration (formulaic NER), US CBP AMS bills-of-lading, US Census Intl Trade API (free, HS-10), UN Comtrade v2 (free), Eurostat Comext, OECD TiVA, AIS (NOAA Marine Cadastre, public domain), GLEIF LEI Golden Copy (free, redistributable), OpenFIGI (MIT), Wikidata (CC0). Avoid OpenCorporates (£12k+/yr) as the public spine.
- **Macro gap (minor):** add the 3m10y spread once DGS3MO is cached, World Bank (CC-BY) + IMF SDMX alongside FRED, and ALFRED vintage ingestion for exact macro-revision PIT.
- **Gotcha:** HS code is NLP-inferred (not in public AMS) — better imputation is a v1 differentiator; keep disclosed edges separate from inferred shipment edges.

---

## 3. Prioritized Sprint Backlog

Ordering rationale: maximize (parity impact × inverse effort). Estimates and reference-classifications are high-impact / low-effort (pure SEC-EDGAR prose + free public taxonomies, no licensing). Insider is high-impact / medium-effort (clean Form-4 XML). Corp-actions and pricing depth are foundational for any return study. ESG and off-exchange are medium. Supply-chain is the highest-effort wedge and goes last. Fundamentals concept-coverage polish is interleaved.

| Sprint | Domain | Headline gap | One-line acceptance criterion |
|:------:|--------|--------------|-------------------------------|
| **1** | Estimates | Populate and extend injectable estimates store | Real licensed/manual estimate detail and consensus files load into `est_detail`/`est_consensus`; recommendation/price-target normalization is covered; accepted vendor crosswalk rows populate `est_security_link` with PIT-safe as-of resolution; 8-K Item 2.02/7.01 NER populates `est_guidance` for >=500 issuers; row counts appear in `lake/est_*/_manifest.json`. |
| **2** | Reference classifications | No taxonomy tables; SIC/NAICS unextracted | `taxonomy`/`taxonomy_node`/`entity_classification`/`sic_code_dim`(authority-keyed)/`naics_code_dim` exist; SEC-assigned SIC + NAICS + Fama-French + TRBC-via-PermID loaded for the full security universe with bitemporal `valid_from`. |
| **3** | Insider ownership | Raw Section 16/13D-G tables, S29 signal metrics, and S30 Form 144 reconciliation exist, but breadth and adjacent public surfaces are thin | Load >=1 quarter broad Form 3/4/5 backfill, populate the built Form 144 intent feed, materialize `insider_transaction_metrics` across that universe, add N-PORT/N-PX/congressional disclosure tables, and pre-2023 10b5-1 inference/household rollups. |
| **4** | Fundamentals (concept coverage) | S4a cross-industry dictionary corrected to 137 item_ids; S4b/S5 overlays and routed industry templates seeded | `fundamental_statement_map` covers exactly the 137 authorized cross-industry item_ids from sections 2.1-2.4 plus 47 bank/insurance/REIT/utility/broker-dealer overlay item_ids; `entity_industry_template` and `industry_template_coverage` govern template assignment and item coverage; `rdq` (8-K 2.02 date) captured; calc-linkbase + DQC validation writes to `data_quality_checks`. |
| **5** | Corp actions + Pricing depth | Valuation-input spine, EV, share float/treasury/class facts, and injectable observed-return resolution exist; live DLRET and quote-depth data remain absent | Load real CRSP/Form 25/15/OTC terminal-return evidence into `delisting_return_observations`, reconcile CRSP DLSTCD, run live EV/valuation-input coverage over the governed universe, then add quote/close-type depth and spinoff `Form 8937` loader stub. |
| **6** | 13F extension + Off-exchange ATS | No filer entity-resolution; no ATS volume | `filer_13f_cik_alias` (subadvisor/M&A rollup) populated; 2023-01-03 ×1000 cutover verified; `offexchange_volume`/`offexchange_venue` loaded from FINRA weeklySummary API with ATS % rollup. |
| **7** | ESG / sustainability | No ESG store | `esg_metric_dim`/`esg_metric`/`ghg_emission`/`esg_score`(redistributable=FALSE)/`esg_controversy` exist; 10-K Item 1/1A + Form SD + CA SB 253 + CDP loaders populate raw disclosed metrics with bitemporal evidence trail. |
| **8** | Fundamentals PIT/QA hardening | No cross-vendor reconciliation | `fact_disagreement` audit table compares XBRL vs Sharadar/SimFin; DERA bulk backfill (SUB/NUM/TAG/PRE) extends pre-API history; >99% agreement target tracked in `data_quality_checks`. |
| **9** | Supply-chain graph | `sc_node`/`sc_edge` absent (strategic wedge) | `sc_node`/`sc_edge` exist; Exhibit-21 subsidiary graph + 10-K Item 1 customer-concentration loaded with `disclosure_type`/`confidence`; CBP-AMS or Census-trade ingest produces tier-1 inferred edges keyed on GLEIF/FIGI. |

**Parity-ledger upkeep (cross-cutting):** every sprint above must add/flip the matching `db/parity.py` `ProviderParityRow` from `partial`/absent to reflect new `warehouse_tables` — currently the ledger claims partial parity for estimates (via `fundamentals_features`) and corp-actions that the schema does not yet substantiate.
